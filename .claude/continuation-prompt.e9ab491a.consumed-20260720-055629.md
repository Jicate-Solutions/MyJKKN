App Routines — dupe-scan-as-moat (self-improving duplicate-detection LOOP). Continuation brief follows.

---

TASK (P0, Director-set, now fully unblocked): Build the App Routines subsystem's `app.dupe-scan` as a real self-improving LOOP (a moat), NOT a one-shot AI report. Adopt the EXACT moat-vs-echo design proven by the MyJKKN cluster self-improving loop (which GRADUATED to `self_improving` on 2026-07-18 — see below). This is the single most important thing for this session. Everything from the prior session carries forward; nothing was dropped.

PROJECT: /Users/omm/PROJECTS/jkkn-centralized-bug-reporter (the reporter repo — "Jicate Apps Control Tower"). NOTE the shell CWD is /Users/omm/PROJECTS/MyJKKN but ALL of this task's work is cross-repo in the reporter. Ships to production via git remote `jicate` = Jicate-Solutions/BugReporter; **merge to `main` auto-deploys prod** (homepage https://jkkn-centralized-bug-reporter.vercel.app). Migrations are files applied by hand via the Mgmt API (NOT auto-applied by CI).

DATABASE (reporter, SEPARATE from MyJKKN): Supabase ref `adakhqxgaoxaihtehfqw` ("Centralized Bug Reporter"). Reach it with the Mgmt token at /Users/omm/.supabase/access-token via `curl` POST to https://api.supabase.com/v1/projects/adakhqxgaoxaihtehfqw/database/query -H "Authorization: Bearer $(cat /Users/omm/.supabase/access-token)" -H "Content-Type: application/json" -d '{"query":"<SQL>"}'. **Use curl, not python urllib** (urllib UA gets a Cloudflare/WAF 403). App env (service-role key etc.): /Users/omm/PROJECTS/jkkn-centralized-bug-reporter/.env. Show SQL first; validate DDL in a rolled-back BEGIN…ROLLBACK batch on live before applying; every new SECURITY DEFINER RPC needs explicit `REVOKE EXECUTE ... FROM anon, PUBLIC` + `GRANT` to the right role, and must self-scope inside its body.

SPEC (the template dupe-scan copies — READ FIRST): /Users/omm/PROJECTS/MyJKKN/.claude/worktrees/loop-spec/docs/features/2026-07-18-FEATURE-cluster-selfimproving-loop.md — the moat-vs-echo checkpoints, the honest gate ledger (m/f gates), the human-gate rules, and the 2-cycle acceptance test are all defined there. Consider running the `/moat-loop` skill's live 2-cycle simulation as the acceptance gate before flipping any gate "on".

PROGRESS: /Users/omm/PROJECTS/MyJKKN/progress.txt (top block = the session that shipped Routines Ph2 + tabbed surface and deferred dupe-scan; its "Next Session — Tasks" mirror this brief).

CURRENT STATE (as of brief-write time):
- Prior session shipped, both LIVE on reporter prod: PR #12 (`0627de9`) App Routines Ph2 UI (`/org/[slug]/routines`); PR #13 (`44d1f3c`) tabbed per-app control surface (`apps/[appSlug]` → shared layout + 5 nested tabs Overview·Bugs·AI·Routines·Uptime, all reusing existing components app-scoped; Routines added to org sidebar).
- dupe-scan was DEFERRED (Director redirect) precisely so it becomes a moat, not an echo.
- The moat SUBSTRATE already exists in the reporter (Rule #26 pre-survey found it — do NOT reinvent): `bug_reports.embedding` (text-embedding-3-small, 1536d) + `find_similar_bugs(target,min,max)` RPC (dup ≥0.9 / related 0.7–0.9) + the propose endpoint /Users/omm/PROJECTS/jkkn-centralized-bug-reporter/app/api/v1/bug-reports/[id]/similar/route.ts + the DISMISS endpoint /Users/omm/PROJECTS/jkkn-centralized-bug-reporter/app/api/v1/bug-reports/[id]/similar/dismiss/route.ts (already writes to `similarity_feedback`) + the ground-truth store table `similarity_feedback` (0 rows, unused).
- ⚠️ SCHEMA DRIFT (critical, [[reference_reporter_bug_reports_schema_drift]] class): the LIVE `similarity_feedback` columns are `{id, bug_report_id, suggested_bug_id, user_id, feedback_type, created_at}` (0 rows) and DIFFER from BOTH the migration file `/Users/omm/PROJECTS/jkkn-centralized-bug-reporter/supabase/migrations/20251231_ai_similarity.sql` (which declares `similarity_score, suggestion_type, dismissed_by_user_id, organization_id, dismissed_at`) AND the dismiss route (which writes those file-schema columns). So the dismiss route as-written may CRASH on the live table — a `.select()`/`.insert()` against a phantom column compiles clean under tsc and throws at runtime. VERIFY the live columns via information_schema before trusting either the file or the route, and reconcile (either migrate the table to the intended schema, or write to the columns that actually exist).
- The cluster self-improving loop (the PATTERN dupe-scan copies) is now GRADUATED and PROVEN, not merely built: on 2026-07-18 ~23:10 IST loop_registry `bug-triage` gates {a,g,m,f} are ALL on, `loop_class=self_improving`, proven on real reporter answers (2 clusters walked the full cycle, both unanimous 👍, outcome ledger 2 positive rows). So the design is safe to copy.

VERIFY CURRENT STATE (run BEFORE building — read-only; if reality differs from this brief, STOP and report, do NOT execute a stale plan):
- Reporter main is at/after #13: `cd /Users/omm/PROJECTS/jkkn-centralized-bug-reporter && git fetch jicate main && git log jicate/main --oneline -3` → expect `44d1f3c` (tabbed surface) present.
- Substrate exists (one curl batch to the Mgmt API above): `select to_regclass('public.similarity_feedback');` (not null) · `select proname from pg_proc where proname='find_similar_bugs';` (1 row) · `select column_name,data_type from information_schema.columns where table_name='similarity_feedback' order by ordinal_position;` → CONFIRM the live columns match `{id,bug_report_id,suggested_bug_id,user_id,feedback_type,created_at}` and reconcile the drift · `select count(*) from similarity_feedback;` (expect 0).
- Cluster loop genuinely graduated (proves the pattern before copying): read /Users/omm/.claude/projects/-Users-omm-PROJECTS-MyJKKN/memory/project_selfimproving_bugfix_loop.md ("GRADUATED" section). If it turns out NOT graduated, or the substrate is missing/renamed → STOP and report rather than building on a stale assumption.
- Reporter builds: `cd /Users/omm/PROJECTS/jkkn-centralized-bug-reporter && npx tsc --noEmit` (ALWAYS exactly 1 pre-existing `resend` error — ignore it) and `npx eslint .`. There is NO test suite.

WHAT NEEDS TO HAPPEN (the dupe-scan moat, mapped 1:1 to the cluster spec):
1. PROPOSE (weak signal, earns NO gate): `app.dupe-scan` routine surfaces embedding-cosine candidate duplicate PAIRS (≥0.9 via `find_similar_bugs`), app-scoped, EXCLUDING already-dismissed pairs. It is recommendation-only — it NEVER auto-closes, auto-merges, or auto-resolves a bug. Display it as "AI-proposed possible duplicate (not human-confirmed)" so no one mistakes it for truth.
2. GROUND TRUTH / the measurement (earns m: off→on): a human CONFIRMs or DISMISSes a proposed pair → written to `similarity_feedback`. The DISMISS half already exists (the route above); the CONFIRM half must be ADDED (a `feedback_type='confirmed'` write + its own anon-locked SECDEF RPC or route). This human verdict is the only ground truth.
3. FEED-FORWARD / what makes it a moat (earns f: off→on): the NEXT scan (a) EXCLUDES already-dismissed pairs (already done in the propose route — keep it), and (b) keys any learning/threshold-tuning on `similarity_feedback` (the MEASURED human verdicts), NEVER on the AI's own past similarity scores. Conditioning on its own scores is the echo the spec explicitly warns against.
4. Honest gate ledger + acceptance: adopt the cluster loop's discipline — measurement earns m:on, measured feed-forward earns f:on, both + a passing 2-cycle acceptance test → "graduate". NOTE: the reporter DB has NO `loop_registry`/Loop Control Tower table (that lives in MyJKKN); DECIDE whether to add a minimal reporter-side equivalent or track the gate ledger in the dupe-scan spec doc — either way keep the honesty, don't skip it.
5. new-bug-digest = WEAK loop fit (no clean ground truth) → keep it a simple periodic report or DROP it. Do NOT force it into a loop; a forced loop is the exact echo the spec warns against.
6. [P1, do not start until dupe-scan ships] Phase 4 App Advisor (needs `applications.repo_url` + a clone-and-analyze recipe). [P2] org-level uptime page has the SAME unchecked-insert false-empty bug that was fixed on the app tab. [note] pixel-eyeball the 5-tab surface is Director-login-gated: `/org/jicate-solution/apps/<app>`.

CONSTRAINTS & RULES:
- Ship ONE increment per PR off a FRESH branch from jicate/main: `git fetch jicate main && git checkout -B <branch> jicate/main`. Run tsc + eslint locally. Open PR. WAIT for advisory. Fix genuine HIGH/consensus-MED. Merge on green — **merge auto-deploys prod → needs Director approval each time**; do not self-merge without it.
- The "Claude Review (advisory)" GH check is the REAL correctness gate (catches logic bugs invisible to green build/tsc/eslint). TIER SPLIT: a PR **touching a migration** escalates to the DEEP ultracode panel which DEGRADES in CI (subagent permission_denials → writes no review file → job goes green-by-design, comment frozen) → verdict UNRETRIEVABLE, you must self-review the diff by hand. A NON-migration PR gets the STANDARD skeptic tier which works and posts a real comment. So split migration and code into separate PRs where practical so at least the code half gets a real skeptic pass. ALWAYS compare the advisory comment's `updated_at` vs the check-run's `completed_at` for the head SHA before trusting the comment (idempotent + eventually-consistent → it can show a STALE prior verdict). See /Users/omm/.claude/projects/-Users-omm-PROJECTS-MyJKKN/memory/feedback_advisory_review_tier_split_and_stale_comment.md.
- Reporter `bug_reports` real schema ≠ its TS type: NO `priority`/`is_resolved` columns; severity = `category='security'`; statuses = new·seen·in_progress·resolved·wont_fix. `.select()` of a phantom column throws at runtime, invisible to tsc → dry-run any new `.select()` against the live DB.
- Reporter auth = X-API-Key (external) / session (internal AI route is session-authed, org-scoped, app_id=`reporter-<orgId>` server-derived). Reporter form zod STRIPS unknown `settings` keys → any new settings key must be registered in the form schema or app edits wipe it.
- Autosave hook commits "wip" in worktrees → soft-reset to merge-base + one clean commit before push; `git diff --name-only jicate/main..HEAD` before opening a PR from a reused worktree.

KEY FILES TO READ FIRST:
- /Users/omm/PROJECTS/MyJKKN/.claude/worktrees/loop-spec/docs/features/2026-07-18-FEATURE-cluster-selfimproving-loop.md (the moat template — moat-vs-echo checkpoints, gate ledger, 2-cycle test)
- /Users/omm/.claude/projects/-Users-omm-PROJECTS-MyJKKN/memory/project_selfimproving_bugfix_loop.md (the proven pattern + its GRADUATED section)
- /Users/omm/.claude/projects/-Users-omm-PROJECTS-MyJKKN/memory/project_jicate_ai_door.md (top paragraph = most current subsystem status + the dupe-scan substrate facts + advisory-tier lesson)
- /Users/omm/PROJECTS/jkkn-centralized-bug-reporter/lib/routines/catalog.ts + /Users/omm/PROJECTS/jkkn-centralized-bug-reporter/lib/routines/registry.ts (the extensible `ROUTINE_CATALOG`/`ROUTINE_KINDS` spine — a new kind = 1 catalog entry + `buildInput` + a client renderer; `readOnly:true` invariant means only read-only kinds auto-run, so the dupe-scan routine PROPOSES only)
- /Users/omm/PROJECTS/jkkn-centralized-bug-reporter/supabase/migrations/20251231_ai_similarity.sql + the two `similar/` routes above (the propose + dismiss contract dupe-scan extends — mind the live-schema drift)
- /Users/omm/.claude/projects/-Users-omm-PROJECTS-MyJKKN/memory/feedback_advisory_review_tier_split_and_stale_comment.md and /Users/omm/.claude/projects/-Users-omm-PROJECTS-MyJKKN/memory/reference_reporter_bug_reports_schema_drift.md

KEY DECISIONS MADE LAST SESSION (Director-locked — do not re-open):
- DEFER dupe-scan to adopt the PROVEN cluster moat pattern rather than build a duplicate-detection loop in parallel. Rationale: the two live in different DBs so there's no code reuse, but copying a design that already passed the 2-cycle honesty test beats re-deriving one and risking an echo.
- REUSE existing components for the tabbed per-app surface (AI cards, BugReportsDataTable, probeApps, RoutinesManager) rather than rebuilding — kept the diff small and converged advisory to "no real findings".
- The moat-vs-echo CORE: feed-forward MUST key on measured human feedback (`similarity_feedback` confirm/dismiss), NEVER on the AI's own past similarity scores. That single distinction is the entire difference between a moat and an echo, and it is why new-bug-digest (no clean ground truth) must NOT be forced into a loop.

APPROACH: (1) Verify state + reconcile the `similarity_feedback` schema drift. (2) Write a short dupe-scan spec mirroring the cluster spec's structure (propose=weak/no-gate, confirm/dismiss=measurement/m-gate, feed-forward-on-measured=f-gate, human gates permanent, 2-cycle acceptance) and get Director sign-off on the gate-ledger representation. (3) Ship in increments, each its own PR: propose routine → confirm mechanism (+ reconcile dismiss) → feed-forward + 2-cycle acceptance. Prove ₹0 on real reporter data at each step.

QUALITY BAR: dupe-scan never auto-closes/merges/resolves; confirm/dismiss are human-only; feed-forward demonstrably changes the next scan BECAUSE a human verdict was recorded (run the 2-cycle falsification: hide the measured row → the next scan must differ; if identical either way, it's an echo and f stays off). Every new SECDEF RPC anon/PUBLIC-revoked. tsc + eslint green (bar the 1 `resend` error). Advisory converged to only LOW/single-lens polish or "no real findings".

DO NOT: force new-bug-digest into a loop · let dupe-scan condition on its own past scores · auto-resolve/merge/close any bug · trust the migration file OR the dismiss route's column list without an information_schema check on live · self-merge without Director approval · combine a migration and its consumer code in one PR if you want a real skeptic verdict on the code · read raw 40KB tool payloads into context (compute-then-print).

VERIFY BY: information_schema confirms the reconciled `similarity_feedback` schema; a real confirm and a real dismiss each write one row; a live dupe-scan run proposes real candidate pairs at ₹0 and excludes dismissed pairs; the 2-cycle test passes (next scan changes because of a measured verdict, and NOT when the verdict is hidden); advisory converged; prod homepage 200 after each merge.
