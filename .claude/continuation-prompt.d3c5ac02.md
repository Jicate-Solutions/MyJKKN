TASK: Wire per-job model selection into the Max lane's execution path so the model column the console now SETS actually RUNS. As of the 2026-07-24 unification, `/admin/ai-models` collapsed into one "AI Jobs" console where the Director can set a model (Opus/Sonnet/Haiku) per job — including a "Set model" upsert for the 16 previously model-less Max-lane jobs. But the historical bug (`reference_max_lane_ignores_model_id_hardcoded_sonnet`) is that the Windows Max-lane drain hardcodes `--model sonnet` and `fn_ai_claim` never handed the job's `model_id` to it — so on `lane='max'` the model column is COSMETIC; every job runs Sonnet regardless. The user ranked "Wire per-job model into Max drain" as the P0 "start here." ⚠️ SCOPING TRUTH: a sibling memory (`project_per_job_model_selection_maxlane`) records that on 2026-07-23 this may have ALREADY shipped end-to-end — `fn_ai_claim` was rewired to pass `provider/model_id/fallback_*` in `spec` (applied prod), the Windows drain was patched + PROVEN honoring Opus, and a `payload._model_override` contract was folded INTO `fn_ai_claim` (DB-only, no drain round-trip); PR #2307 merged+deployed. The two memories disagree because the "decorative" reference is the pre-fix DIAGNOSIS (dated 04:45Z) and the fix landed later that day. THEREFORE task 1 is VERIFY-FIRST, not build-first: re-derive what is genuinely still broken before touching anything, and remember the drain lives on the AI-Max Windows box (NOT in the Jicate-Solutions/MyJKKN repo) — that half is not PR-able.

HOW TO READ THIS BRIEF (hybrid intent/state model — the git log is ground truth):
  ▸ DURABLE INTENT (trust as written): the RANKED PRIORITIES, DROP list, KEY DECISIONS, MUST-READ.
  ▸ DERIVED STATE (may be stale — re-derive at resume): CURRENT STATE, branch/PR/DB-fn status, "already shipped?" claims.
  ▸ DERIVE-MODE: git

PROJECT: /Users/omm/PROJECTS/MyJKKN
DATABASE: Supabase prod ref `kvizhngldtiuufknvehv`. Mgmt token at `~/.supabase/access-token`. Run SQL via `curl -sS -A "supabase-cli/2.75.0" -X POST https://api.supabase.com/v1/projects/kvizhngldtiuufknvehv/database/query -H "Authorization: Bearer $(cat ~/.supabase/access-token)" -H "Content-Type: application/json" -d "$(jq -Rs '{query:.}' <<<'SELECT 1')"`. Real service key for local dev = `/Users/omm/PROJECTS/MyJKKN/.env.production.local` (`SUPABASE_SERVICE_ROLE_KEY`) — `.env.local`'s is empty. Deploy ships CODE not migrations — apply any migration yourself via the Mgmt API and validate in a `BEGIN…ROLLBACK` batch first (`feedback_deploy_ships_code_not_migrations_management_api_apply`).
SPEC: none — these are infra/analysis tasks, no spec file.
PROGRESS: /Users/omm/PROJECTS/MyJKKN/progress.txt (top entry = the AI-Jobs unification session that produced this brief).

CURRENT STATE (derived — re-verify):
- `/admin/ai-models` unified "AI Jobs" console LIVE on jkkn.ai (prod build `06844c78e`), browser-verified as super-admin: 42 Max + 3 API jobs, "Set model" on the 16 model-less Max rows, first-time dialog provider-locked to Anthropic. Shipped #2315 (unify, net −184 lines) + #2319 (first-time governance upsert), both merged + deployed.
- Per-job-model-into-Max-drain: per `project_per_job_model_selection_maxlane`, `fn_ai_claim` was rewired + Windows drain patched + PR #2307 merged/deployed 2026-07-23 — i.e. possibly DONE. Treat as UNVERIFIED until you re-derive (below).
- Branch (this shared checkout): `feat/campus-living-fee-compute-engine` — 100+ sibling worktrees on disk from other panes; this is a SHARED tree, do NOT blind `git add -A`. Merged AI-unify worktrees `.claude/worktrees/ai-unify` + `ai-firsttime` are safe to `git worktree remove` BY EXACT PATH only.

VERIFY CURRENT STATE (run FIRST, unconditionally, before any build):
1. `cd /Users/omm/PROJECTS/MyJKKN && git fetch jicate main -q && git log -6 --format='%cd · %s' --date=short` — reconcile against what actually landed; a "MERGED" label is not proof (a squash can orphan a fold-in — content-verify `main`).
2. Read the LIVE `fn_ai_claim` body (source of truth, not repo files which drift): `SELECT pg_get_functiondef('public.fn_ai_claim'::regproc);` via the Mgmt curl. CONFIRM whether the returned `spec` JSON already includes `provider / model_id / fallback_provider / fallback_model_id` and whether it honors `payload._model_override`. If yes → the DB half is DONE.
3. Confirm the registry actually carries per-job models: `SELECT lane, count(*) FILTER (WHERE model_id IS NOT NULL) AS with_model, count(*) FROM ai_job_types GROUP BY lane;` — expect ~42 Max / 3 API. Cross-check that the api lane honors model via `getModelForFeature` (`lib/services/platform/ai-model-config-service.ts`) — that path is NOT the bug.
4. The drain code (`ai-jobs-drain.mjs` `:139/:183`, `ai-chat-drain.mjs` `:290`) lives on the Windows AI-Max box (`~/jkkn-max-lane`), NOT this repo — you cannot grep or patch it here. Its state is only knowable empirically (enqueue a probe job pinned to a non-Sonnet model, read back `ai_jobs.result` for the recorded model).

WHAT NEEDS TO HAPPEN (rank order = user ground truth 2026-07-24; user wins over any progress contradiction):
1. [P0 — VERIFY-THEN-PARTIAL-BUILD] Per-job model into the Max drain. Re-derive (steps 2–4) whether it's already live. If a genuinely repo-side gap remains (e.g. an app-side guard, a `fn_ai_claim` refinement, an audit/record surface, a `max_inflight` read the resolver ignores) → build THAT slice to a Ready PR. If the only remaining gap is the off-repo Windows `.mjs` drain → STOP and report; that is not autobuildable, hand back a paste-prompt for the Director's box instead. Do NOT free-build if there's no PR-able slice.
2. [P1 — LARGE INFRA, partly off-repo] Harden the Max lane (the ₹0 self-hosted equivalents of Managed-Agent benefits): staleness-reclaim + heartbeat to kill the wedge class (`feedback_maxlane_drain_stale_lock_wedge_looks_like_dead_box`), worker-pool concurrency honoring `max_inflight`, structured per-job traces. Much of this is drain-side (off-repo). Carve only the repo-side slices (DB reclaim RPC, trace schema, a monitoring surface) to a PR; flag the box-side pieces as paste-prompts.
3. [P2 — ANALYSIS ONLY, NO PR] Price `scf.note_safety_judge` (1,932 calls/mo, ₹0 today) Max-vs-Managed. Pull the job's real avg input/output tokens from `ai_model_usage` (ts col `invoked_at`), compute cost at CURRENT Sonnet API token rates, add the Claude Managed Agents per-session-hour runtime fee — VERIFY the rate (sources disagree $0.08 vs $0.25 — `reference_claude_managed_agents`; check the official pricing page). Output a written rupee delta, not a PR. Note MyJKKN jobs are short stateless one-shots → session-hour fee ≈ rounding error; the real Managed-only win is the terms boundary for live-user serving.
- Deferred backlog (NOT in the lead set): a third `managed` lane routing to Claude Managed Agents for the terms-gated live-user-facing slice ONLY — needs task 3's pricing first.

CONSTRAINTS & RULES:
- JKKN terminology: "learner" not "student", "staff" not "employee" — the CI terminology gate fails PRs on `Student` labels.
- Any NEW SECURITY DEFINER RPC migration MUST include explicit `REVOKE EXECUTE … FROM anon, PUBLIC; GRANT … TO authenticated` (or `service_role` for cron/system-only fns) — Supabase's default grants anon EXECUTE on every new function.
- Stay in scope (rule #22): touch only what the task requires; the diff must be minimal and traceable.
- Browser-verify any UI change as super-admin before calling it done (rule #14/#2); text/HTTP 200 is not proof.
- Shared checkout: never `git add -A`; ship via a fresh worktree off `jicate/main` (`git fetch jicate main && git checkout -B <branch> jicate/main`), verify `pwd` is this repo before spawning any worktree agent.
- Show SQL before applying; validate rolled-back on prod first.

KEY FILES TO READ FIRST:
- `~/.claude/projects/-Users-omm-PROJECTS-MyJKKN/memory/reference_max_lane_ignores_model_id_hardcoded_sonnet.md` (the diagnosis — pre-fix)
- `~/.claude/projects/-Users-omm-PROJECTS-MyJKKN/memory/project_per_job_model_selection_maxlane.md` (the fix that may have already shipped — READ CAREFULLY, it changes what's left)
- `~/.claude/projects/-Users-omm-PROJECTS-MyJKKN/memory/reference_claude_managed_agents.md` (pricing + strategic split for task 3)
- `~/.claude/projects/-Users-omm-PROJECTS-MyJKKN/memory/project_ai_models_tab_unification.md` (what shipped this session)
- Live DB: `pg_get_functiondef('fn_ai_claim')`, `fn_ai_job_type_set_model`; `lib/services/platform/ai-model-config-service.ts` (`getModelForFeature`)
- `/Users/omm/PROJECTS/MyJKKN/app/(routes)/**/ai-models` route + `_components` (usage panel, edit dialog, switch-watch card)

KEY DECISIONS MADE THIS SESSION (with rationale):
- Demoted `ai_model_config` to a silent resolver-fallback + audited write-store rather than ripping out its write path — preserves the `ai_model_config_audit` trail and the `fn_ai_job_type_set_model` mirror (the real chokepoint for `ai_job_types.model_id` writes); no migration, fully reversible.
- Folded first-time governance into the SAME model-edit PATCH via UPSERT (creates an `ai_model_config` row when none exists) rather than a new endpoint — the `ai_model_config_write_super_admin` FOR ALL RLS already permitted the insert, so the 16 model-less Max jobs became governable with zero new surface.
- Grouped ALL 45 jobs by lane with authoring behind each row's "Advanced" menu (governance-first) — Director's explicit direction; the two tabs were only ever two lenses on one `ai_job_types` registry.
- Re-landed the orphaned #2319 fold-in by cherry-pick after discovering (via content-verifying `main` — `isCreate=0` in the code, NOT the MERGED label) that #2315 squash-merged on commit-1 only. Lesson carried: verify by content, never by the merge label.

APPROACH: Reconnaissance-first (it paid off all session — every DB prediction held live). For task 1, the first move is a read-only DB introspection of `fn_ai_claim` + a pinned-model probe job, NOT a code edit — because the fix may already be live and the only remaining gap may be off-repo. Only after you know exactly what's broken do you decide build-vs-report. Route any MyJKKN PR build through `/myjkkn-chain`'s BUILD DEPTH GATE.

QUALITY BAR: A repo-side change reaches a Ready (non-draft) PR with `npm run build` green, Step 2.7 gates green (nav-config, radix-empty, permissions-catalog, SECDEF anon-revoke, terminology), and — if it touches UI — browser-verified as super-admin. An analysis task produces a written, sourced rupee breakdown. "Verified" means empirical proof (a probe job's recorded model, a curl matrix, a screenshot), not a green label.

DO NOT: merge; deploy; `git add -A` on the shared checkout; patch the Windows drain from here (it's off-repo); assume "MERGED" = live; set a model on a Max job and assume it runs without an empirical probe; ship a task-3 PR (it's analysis only); build task 1 before re-deriving what's actually left; carry a placeholder service key into local dev (login bounces).

VERIFY BY: (1) `pg_get_functiondef('fn_ai_claim')` shows whether model fields + `_model_override` are already handled; (2) a probe job pinned to `claude-opus-4-8` or `claude-haiku-4-5` records that exact model back into `ai_jobs.result` (proves the drain honors it) — or doesn't (proves the gap is still open); (3) for any repo PR: build green + Step 2.7 green + super-admin browser render; (4) task 3: a written Max-vs-Managed rupee delta with the session-hour rate sourced from the official page.

EXECUTION DIRECTIVE (autobuild = YES, with teeth):
Run autonomously, but the loop's FIRST action is the VERIFY CURRENT STATE block above — because task 1's remaining work may be entirely OFF-REPO (Windows drain) and task 3 is analysis-only, you must PROVE there is a genuinely repo-buildable slice before building anything. PRECONDITION GATE: if re-derivation shows the per-job-model path is already live end-to-end and the only gap is the off-repo drain (or there is no gap) → STOP and report findings + hand back a Director paste-prompt for the box; do NOT invent adjacent work to fill the loop. If a real repo-side slice exists (task 1 refinement, or a task-2 DB/trace/monitoring slice) → build it through `/myjkkn-chain`'s BUILD DEPTH GATE to a Ready PR. If the slice is large or ambiguous → offer `/writing-plans` first rather than free-building. For task 3 → produce the written cost breakdown, no PR. NEVER merge, NEVER deploy — the terminal state for repo work is a Ready PR awaiting the Director; for analysis it is a written rupee delta. Prefer STOP-and-report over speculative building whenever the repo-side scope is thin or absent.
