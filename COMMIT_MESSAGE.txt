fix(learners): Comprehensive learner-profile sync enhancement

Fixed three critical issues with learner-profile synchronization:

1. Email updates not syncing to profiles table
   - Enhanced syncProfileStatus() with smart lookup (email + learner_id fallback)
   - Syncs ALL fields: email, role, is_active, learner_id, org data

2. User roles stuck as 'guest' instead of 'student'
   - Auto-corrects role to 'student' in sync function
   - Database trigger ensures consistency

3. No mismatch detection or repair capability
   - Created diagnostic script to detect all mismatches
   - Created repair script to auto-fix issues

Implementation:
- Enhanced service layer (lib/services/learner-profile-service.ts)
- Added 2 database triggers for automatic sync
- Created diagnostic & repair tools

Database changes:
- sync_learner_email_to_profile() function (auto-syncs email, role, org)
- sync_learner_status_to_profile() function (auto-syncs is_active)
- trg_sync_learner_email_to_profile trigger (on INSERT/UPDATE college_email)
- trg_sync_learner_status_to_profile trigger (on UPDATE lifecycle_status)

New scripts:
- scripts/debug-learner-profile-sync.ts (detect mismatches)
- scripts/repair-learner-profile-sync.ts (auto-fix issues)
- scripts/LEARNER_PROFILE_SYNC_GUIDE.md (complete guide)

Documentation:
- docs/fixes/2026-01/2026-01-28-FIX-learner-profile-sync-issues.md (root cause)
- IMPLEMENTATION_COMPLETE.md (deployment guide)
- Updated supabase/SQL_FILE_INDEX.md

Benefits:
✓ Email changes automatically sync
✓ User roles always correct
✓ Status changes propagate immediately
✓ Orphaned profiles auto-linked
✓ Comprehensive logging & diagnostics
✓ Self-healing system

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>
