/**
 * Attention Bar — public barrel.
 *
 * Spec: specs/attention-bar-5-layer-system.md (Phase 2 — pill component).
 *
 * Consumers should import from this module rather than reaching into the
 * internal file paths so we can refactor the implementation freely.
 */

export { AttentionBar } from './attention-bar';
