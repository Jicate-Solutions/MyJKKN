TASK: Advance ALL THREE threads of the reporter dupe-scan self-improving loop (moat) — per the Director interview verdict "all 3 of them, carry everything." The three threads, in natural order: (1) apply the reporter `loop_registry` migration to prod (Director go given) and verify the Loops Control Tower screen once deployable; (2) fix the org GitHub Actions billing block (Director/user action) → merge PR #14 (Inc-1 Propose) + PR #15 (Loop Tower) → deploy-verify → flip the `app.dupe-scan` gate `a:on` via a small migration; (3) build Increment 2 (admin-gated confirm/dismiss → `similarity_feedback`, record-only, editable, + fix the crash-broken per-bug dismiss route), which earns the `m` gate, then Increment 3 (feed-forward strictest-wins + 2-cycle) to earn `f` and graduate the loop to `self_improving`. Do not force a single choice — all three threads are live; threads 1 and 3 can proceed independently, thread 2 gates on the billing fix (user's action).

PROJECT: repo `jkkn-centralized-bug-reporter`, remote `jicate` = Jicate-Solutions/BugReporter. Merge to `main` auto-deploys prod via Vercel (Vercel billing is SEPARATE from GitHub Actions — a merge WOULD deploy even while Actions is blocked). Shell CWD is `/Users/omm/PROJECTS/MyJKKN` but ALL work here is cross-repo in the reporter — MyJKKN `main` stays untouched. Reporter worktrees already exist: `.claude/worktrees/dupe-scan` (PR #14 branch) and `.claude/worktrees/loop-tower` (PR #15 branch, holds the migration SQL). Reporter sidebar = `components/dashboard/app-sidebar.tsx` (`mainNavItems`); Control Tower screens live at `app/(dashboard)/org/[slug]/<name>/page.tsx`.

DATABASE: reporter Supabase project ref `adakhqxgaoxaihtehfqw` — a SEPARATE database from MyJKKN's (`kvizhngldtiuufknvehv`); do not cross them. Mgmt token at `/Users/omm/.supabase/access-token`; use **curl only** (urllib/python UA gets WAF-403'd). There is **NO local `.env`** in the reporter — keys live on Vercel; fetch anon/service keys via Mgmt API `GET /v1/projects/adakhqxgaoxaihtehfqw/api-keys?reveal=true` to test PostgREST (anon key validates a FK-embed: 200 `[]` = valid relationship, 400 = bad). Migrations are applied BY HAND via the Mgmt API `POST /v1/projects/<ref>/database/query` (NOT by CI). Every new SECURITY DEFINER RPC must include explicit `REVOKE EXECUTE ... FROM anon, PUBLIC`.

SPEC: `/private/tmp/claude-501/-Users-omm-PROJECTS-MyJKKN/c343c70c-3f0f-488f-b3f2-c8e6ca910609/scratchpad/2026-07-20-app-dupe-scan-loop-spec.md` (full locked spec — success sentence, the loop, all 8 decisions, aggregation rule, honest gate ledger, increment plan). Note: this scratchpad path is session-tmp and may not survive a machine reboot — if it is gone, the authoritative fallback is the memory file `project_reporter_dupescan_moat.md`.

PROGRESS: `/Users/omm/PROJECTS/MyJKKN/progress.txt` — the relevant entry is `## Session: 2026-07-20 (END /cnext) — Reporter dupe-scan MOAT started + Loop Control Tower built`. (Newer top entries in the file are a DIFFERENT sibling lane — the SCF note-safety judge — not this thread; do not conflate.)

CURRENT STATE:
- **PR #14 (Inc-1 Propose)** — BUILT + VERIFIED, **NOT merged**. Evolved `AiDuplicateFinderCard` into an honest proposer (≥0.9 cosine, labeled "not human-confirmed", excludes decided pairs team-wide) via NEW service-role route `GET /api/internal/bug-reports/decided-pairs` (needed because `similarity_feedback` RLS is per-user). Verified tsc/eslint/embed-syntax(200)/join-semantics(rolled-back).
- **PR #15 (Loop Control Tower)** — BUILT + VERIFIED, **NOT merged**. New `loop_registry` table mirroring MyJKKN (gates {a,g,m,f}, `loop_class`, `stack_tier`, `domain`, `routine_id`, `counter_metric`) + RLS (authenticated read, service-role write, anon revoked) + `/org/[slug]/loops` gates screen + sidebar "Loops". Seeded `app.brief` (cadence, a:on) + `app.dupe-scan` (intake, all off).
- **`loop_registry` migration** — validated ROLLED-BACK clean, but **NOT applied to prod** (the classifier blocked the DDL curl → needed explicit Director go, now GIVEN via the interview).
- **8 Director decisions LOCKED** (see below).
- 🔴 **BLOCKER: GitHub Actions billing failed org-wide (Jicate-Solutions)** — advisory router fails in ~3s, billing message lives in the check-run ANNOTATION (not logs; logs 503'd during a concurrent GH incident that masked it). Blocks advisory/CI on ALL Jicate repos, so PRs #14/#15 can't get their advisory. Vercel deploy is UNAFFECTED. Director fixes in org Billing & plans.

VERIFY CURRENT STATE (read-only checks to run BEFORE any work — if reality differs, STOP and report):
1. `gh pr view 14 --repo Jicate-Solutions/BugReporter --json state,mergeable,baseRefName` and same for `15` — confirm both still OPEN, base `main`, not silently merged/closed.
2. Check whether billing cleared, using the annotation recipe from the billing lesson: `SHA=$(git rev-parse <pr-branch>)`; `CRID=$(gh api "repos/Jicate-Solutions/BugReporter/commits/$SHA/check-runs" --jq '.check_runs[] | select(.name|test("advisory|router";"i")) | .id')`; `gh api "repos/Jicate-Solutions/BugReporter/check-runs/$CRID/annotations" --jq '.[0].message'`. Also `gh run list --repo Jicate-Solutions/BugReporter --limit 12` — if ONLY the PR runs are red and older runs green, it's still billing; if many unrelated runs are red it's a broad GH incident.
3. `git fetch jicate main && git log jicate/main --oneline -5` — see what's live; confirm neither PR's files already landed.
4. Confirm `loop_registry` exists-or-not on prod via Mgmt API: `curl` `POST /v1/projects/adakhqxgaoxaihtehfqw/database/query` with `{"query":"SELECT to_regclass('public.loop_registry');"}`. NULL = not applied yet (expected); non-null = already applied (STOP and reconcile before re-applying).

WHAT NEEDS TO HAPPEN (numbered — the 3 threads):
1. **Apply the `loop_registry` migration to prod** (Director go given). SQL lives in `.claude/worktrees/loop-tower/supabase/migrations/20260720_loop_registry.sql`. Show-SQL-first, then validate ROLLED-BACK (BEGIN…ROLLBACK batch), then apply via Mgmt API. Verify post-apply row state (the two seeded rows `app.brief` a:on + `app.dupe-scan` all-off present) AND that anon CANNOT select `loop_registry` (anon-locked, per the RLS/revoke). Then verify the `/org/[slug]/loops` Loops screen renders once PR #15 is deployable.
2. **Fix billing → merge #14 + #15 → deploy → flip `app.dupe-scan` a:on.** Billing fix is the USER's action (org Billing & plans) — do not attempt a code workaround. Once billing clears and advisory runs (or self-review per the constraint), Director merges #14 + #15 (each merge auto-deploys prod). After #14/#15 land, flip the `app.dupe-scan` gate `a:on` via a SMALL follow-up migration (gate flips are migrations, mirroring MyJKKN — never edit the row ad-hoc).
3. **Build Increment 2 (earns `m`) then Increment 3 (earns `f` → graduate).** Inc 2 = admin-gated (D2) Confirm/Dismiss buttons on proposed pairs → write `similarity_feedback` (`confirmed_duplicate`/`dismissed`, `user_id=auth.uid()`); **record-only, never auto-close** (D3); verdict **updatable** (E2, upsert on the `(bug_report_id, suggested_bug_id, user_id)` unique key); **+ FIX the crash-broken per-bug dismiss route** `app/api/v1/bug-reports/[id]/similar/dismiss/route.ts` (currently writes 5 phantom columns + wrong `onConflict` → 500s live) so it writes the LIVE columns. Inc-2 writes are session-client inserts (RLS INSERT allows own-row) = non-migration. Inc 2 STACKS on #14 → build it AFTER #14 merges, OR rebase onto `jicate/main` post-merge (plan the rebase; do not blind-stack). Then Inc 3 = feed-forward: exclusion already team-wide (Inc-1 route), learning aggregates per-pair strictest-wins (E1: any dismiss = not-clean) never past cosine scores, + 2-cycle falsification (hide a verdict → next scan must differ) → earns `f` → migration flips `f` → `loop_class → self_improving`.

CONSTRAINTS & RULES:
- **NEVER self-merge** — Director merges each PR; a merge auto-deploys prod. You open/verify, Director merges.
- Migrations are applied BY HAND via Mgmt API, NOT by CI. Always show-SQL-first + rolled-back validation before applying.
- The classifier blocks prod DDL curls → any apply needs the explicit Director go (given for `loop_registry`; a NEW gate-flip migration needs its own go if the classifier blocks it).
- `similarity_feedback` RLS is per-user (`auth.uid()=user_id`, SELECT/INSERT/DELETE) — **cross-user reads need a service-role route** (why the `decided-pairs` route exists), NOT the browser client.
- Reporter supabase client is untyped enough that new tables need the cast `createClient() as unknown as SupabaseClient` to `.from('<new table>')` without generated-type churn.
- The autosave hook commits `wip` in worktrees → soft-reset/amend before push.
- Advisory has 2 tiers: on a **migration PR** the DEEP panel DEGRADES in CI (no verdict file → self-review by hand); a non-migration PR gets the working skeptic tier. Split migration and consumer into separate PRs so the non-migration half gets a real skeptic.
- Billing block stops advisory/CI on ALL Jicate repos but **Vercel deploy still works** — "can we ship" and "did the skeptic run" are different questions.
- tsc + eslint must be green (ignore the 1 pre-existing `resend` error).

KEY FILES TO READ FIRST:
- The 4 sources above (progress.txt entry, `project_reporter_dupescan_moat.md`, `feedback_github_actions_billing_fails_as_router_crash.md`, the scratchpad spec).
- The two `similar/` routes: `app/api/internal/bug-reports/decided-pairs/route.ts` (Inc-1 service-role route, reuse the org-membership + `createAdminClient()` pattern) and `app/api/v1/bug-reports/[id]/similar/dismiss/route.ts` (crash-broken, Inc-2 fixes it).
- The loop-tower worktree files: `.claude/worktrees/loop-tower/supabase/migrations/20260720_loop_registry.sql` + the `/org/[slug]/loops` page + the sidebar edit.

KEY DECISIONS MADE (Director-locked — do NOT re-open):
- **D1** Loop visibility → BUILD a reporter-side Loop Control Tower (a visible gates screen, full parity with MyJKKN) — NOT a spec-doc-only ledger.
- **D2** Who confirms/dismisses → admins/owners who manage bugs ONLY — not any signed-in viewer.
- **D3** What confirm does → record the verdict ONLY — never auto-close the bug (closing stays a separate deliberate human action).
- **D4** Cross-app pairs → same-app ONLY — never pair bugs from two different apps.
- **E1** Two admins disagree → any 'dismiss' = NOT a clean duplicate (strictest-wins; a false "same" is the costly error).
- **E2** Change your mind → allowed (flip your own verdict; upsert on `(bug_report_id, suggested_bug_id, user_id)`, learning re-derives).
- **E3** Undecided pairs → keep re-showing until someone decides (no snooze in v1).
- **E4** When it runs → on-demand button ONLY (no scheduled routine in v1).
- probe_verdict this session = **healthy** (context was intact; these locks are firm).

APPROACH: Advance all 3 threads. Thread 1 (apply `loop_registry` migration) and thread 3 (build Inc 2) can proceed now — thread 1 needs only the show-SQL-first + rolled-back-validate + apply + verify sequence; thread 3's build can start but its PR STACKS on #14 (build after #14 merges or plan the rebase). Thread 2 (merge #14/#15) gates on the billing fix, which is the user's action — do NOT poll GitHub in a tight loop; use a Monitor on the check-run/billing annotation so the merge step fires when billing clears, and keep working thread 1 + thread 3 meanwhile.

QUALITY BAR: Each PR advisory-clean once billing is back, OR self-reviewed by hand if the migration-PR advisory degrades. The `loop_registry` migration is proven ONLY by post-apply row state (2 seeded rows present) + anon-cannot-select. Inc 2 confirm/dismiss is proven by a REAL `similarity_feedback` row written then rolled-back (one confirm + one dismiss, each a single row, correct `feedback_type` and `user_id`). Gates flip ONLY via migration, never by editing the row in place.

DO NOT: self-merge any PR; apply ANY migration without show-SQL-first + rolled-back validation; build Inc 2 blind-stacked without planning the rebase onto post-merge `jicate/main`; condition the dupe-scan learning on its own cosine scores (it learns ONLY from measured human verdicts); auto-close/merge/resolve any bug on confirm (D3); overclaim gates (`m`/`f`/`self_improving` are earned by shipped increments + 2-cycle proof, not by intent).

VERIFY BY (post-execution checks): `loop_registry` rows present on prod (`app.brief` + `app.dupe-scan`) AND anon `SELECT` on it returns denied/empty; the `/org/[slug]/loops` screen renders once #15 deploys; #14 and #15 show `state=MERGED` with `baseRefName=main` AND `git ls-tree jicate/main` confirms the new files actually landed on main (stacked/base-branch merge trap — verify files, not just the merge click); a real confirm + a real dismiss each write exactly one `similarity_feedback` row (rolled-back proof); the `app.dupe-scan` gate `a` flips on after #14 deploys.

---
*This brief auto-loads on `/clear` in this pane (SessionStart hook reads `.claude/continuation-prompt.<session-key>.md`). It is the sole context the next session gets — execute on "go."*
