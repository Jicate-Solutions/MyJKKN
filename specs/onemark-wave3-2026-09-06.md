# OneMark — Wave 3 lane specs (decisions of record; mirror of .claude/onemark-wave3-specs.md) (prepared 2026-09-06 00:50 IST; launch AFTER the Wave 2 bank holds ≥ 50 approved items per subject OR on Director word)

Wave 3 = "make it real": the PRD's Phase 3 (Senior Learner analytics), the debt Wave 2's reviewers named, and the operations that need people, not code. Nine code lanes + one operations lane. Wave 2 shipped 2026-09-05 (main `304b3314b2`+, live `cee167f8ae`); everything below builds on that.

Common to every lane (read first, obey exactly):
- Rulings: `specs/onemark-decisions-2026-09-02.md` (20 decisions) + `specs/onemark-wave3-2026-09-06.md` (this file, on jicate/main). Schema you build on: `20260917111500` (Wave 1), `20260918101500` + `20260918130000` (Lane S + hardening), `20260918120000` (draft enable) — all APPLIED in prod. `types/onemark.ts`. Read them, do not re-derive.
- STEP 0: `git fetch jicate main && git checkout -B <branch> jicate/main`. STEP 0.5: production-code sweep (`git ls-tree jicate/main -r --name-only | grep -iE "onemark|foundation/(practice|console)|fp_"` + `gh pr list --search "onemark in:title" --state all --limit 30`), paste into PR body; if it already exists, return `already_exists`.
- OWN ONLY the files named under your lane. NEVER touch: `lib/services/foundation/foundation-service.ts`, `hooks/foundation/use-foundation.ts`, `app/(routes)/foundation/_components/{cohort-console,enroll-learner-dialog}.tsx`, `lib/ai-tasks/registry.ts`, `supabase/**` unless you are Lane S3, `lib/sidebarMenuLink.ts` + `lib/foundation/guide/**` unless you are Lane N, `lib/onemark/pdf/**` unless you are Lane D, `lib/services/onemark/attempt-server.ts` + `app/api/foundation/onemark/attempts/**` unless you are Lane L.
- Reuse, never rebuild: `fp_items/fp_assessments/fp_attempts/fp_responses/fp_cohorts/fp_students`, `onemark_mistake_vault`, `onemark_category_weights`, `exam_topic_map`, `fn_get_policy_int(key, default)` for every `onemark.*` policy, `fn_fp_can_view_student`, `fn_fp_manages_school`, `fn_fp_student_progress` (learner-level progress already exists — Lane A wraps it, does not copy it), `fn_fp_recompute_weakness`, `lib/utils/bos/pdf-fonts.ts`, `@/lib/supabase/server` two-client rule (identity via session client, answer keys via service-role; a learner never receives `fp_items.answer` before responding — decision 7).
- Terminology gate is BLOCKING on all new .ts/.tsx/.md prose — the mapping lives in `scripts/ci/check-terminology-delta.py` (learner, Senior Learner, learning framework, score, unit list, learning studio are the house words). Identifiers exempt. `python3 scripts/ci/check-terminology-delta.py jicate/main HEAD` — always exits 0; empty report = pass. Test fixtures count: never use a forbidden word inside a fixture value, even as part of a permission key (Lane R lost a round to one); the gate does not exempt backticks.
- Non-Latin text (rule #24): any Tamil string longer than 5 words is `[TAMIL_TBD: <english>]` until a native reviewer fills it; ship with English fallback; list every placeholder in the PR body.
- Scoped tsconfig INSIDE the worktree (`NODE_OPTIONS=--max-old-space-size=8192`); delete before commit. UI lanes: `npm run build` must exit 0 — count real builds with `ps aux | grep '[.]bin/next build'` and never start one while another runs on this Mac. Mirror `scripts/ci/check-nav-config-hrefs.sh`, `check-radix-select-empty-values.sh`, `node scripts/check-permissions-catalog.mjs`. Hub Page Reachability + Guide deep-links evaluate the PR's FROZEN merge ref: after a dependency merges, `update-branch` (PUT), never `gh run rerun`. `lib-unit-suite` has no ready_for_review trigger — mirror it locally if you were Draft when you pushed.
- No new permission keys — reuse `foundation.*` (practice.take = learner; items.manage = author/approve; assessments.manage = build papers + read cohort results; the learners-manage key under `foundation.*` in `lib/constants/permissions.ts` + owner row = enrol; `cohorts.view` = see rosters).
- Every new SECURITY DEFINER function: `REVOKE EXECUTE ... FROM anon, PUBLIC; GRANT ... TO authenticated;` (+ `REVOKE FROM service_role` for internal helpers, as `20260918130000` does). Migrations: rehearse on prod in BEGIN…ROLLBACK (Python body → `curl -d @file`) + SEPARATE post-rollback check; NEVER apply — the coordinator applies (ledgered) BEFORE merge.
- Explicit `git add` paths; never `-A`, never `stash`; expect `wip: auto-save`, reset --soft and recommit. Fast-forward pushes only. PR READY unless it carries [risky] items (then Draft + list them). NEVER MERGE.
- Every lane ends with: PR number, files, check tally by `status==completed`, and `[safe]`/`[risky]` assumptions.

Sequencing: **S3 first** (apply before merge; A, L, T, D contract-depend on it) → A, T, D, L, Q, U, G in parallel (disjoint files; Q's one-line change to POST /attempts lands through L, U's one-file change to /cdc/admin/exam-topic-map is coordinated) → **N last** (nav + guide need the pages to exist). Lane 0 runs throughout and is people, not code.

---
## Lane S3 — `feat/onemark-w3-schema` — THE ONLY LANE WITH SQL
One migration `supabase/migrations/2026091914xxxx_onemark_wave3_schema.sql` (distinctive timestamp; run `check-migration-version-cross-pr.sh`) + `SQL_FILE_INDEX.md` row + `types/onemark.ts` additions. Rehearse BEGIN…ROLLBACK + separate rollback check; DO NOT APPLY.
1. **Served-set persistence (replaces Lane V's HMAC token).** `ALTER TABLE fp_attempts ADD COLUMN served_item_ids uuid[]` (NULL for live papers, which use `fp_assessment_items`). `fn_onemark_record_response` and `fn_onemark_finalize_attempt`: when `served_item_ids IS NOT NULL`, refuse (RAISE 22023) any `p_item_id` / named skip outside the array — the server-side wall Lane V's token approximates. Keep the token path working until Lane L removes it (both checks can coexist).
2. **Abandoned live sittings.** `fn_onemark_close_abandoned_live() RETURNS int` — SECURITY DEFINER, service-role-only (REVOKE from authenticated too; the cron route calls it with the service client): every `fp_attempts` `mode='live'`, `status='in_progress'` whose paper's `config.close_at + fn_get_policy_int('onemark.live.auto_close_after_minutes', 30) minutes < now()` gets its unanswered `fp_assessment_items` backfilled as skips, then `fn_onemark_finalize_attempt`. Returns the count. Idempotent.
3. **Cohort results RPCs** (the PRD's Phase 3 analytics): `fn_onemark_cohort_results(p_assessment_id uuid) RETURNS jsonb` — caller must hold `foundation.assessments.manage` AND `fn_fp_manages_cohort_school(cohort_id)`; per learner: score, submitted_at, status, taken-digitally flag (decision 17), per-tag correct/total, per-unit correct/total; per item: p-value (fraction correct) and the most-chosen wrong option (distractor analysis). `fn_onemark_learner_report(p_student_id uuid, p_exam_definition_id uuid) RETURNS jsonb` — caller `fn_fp_can_view_student`; wraps `fn_fp_student_progress` and adds vault state (active/mastered counts, next due) and last 10 sittings. NEVER return `fp_items.answer` text in either payload — return option KEYS only, and only for submitted attempts.
4. **Per-person interface language (decision 5).** `onemark_user_prefs(user_id uuid PK REFERENCES auth.users, ui_locale text NOT NULL DEFAULT 'en' CHECK (ui_locale IN ('en','ta')), updated_at)`, RLS: own row read/write only. No column on `profiles` (it has no preferences column and is shared by every module).
5. **Question images.** Storage bucket `onemark-question-assets` (private; max 2 MB per object; RLS: read for `authenticated` holders of `foundation.practice.take` OR `foundation.items.manage`; write for `foundation.items.manage`). `onemark_question_assets` already exists (item_id, asset_type, storage_path, alt_text, sort_order) — add RLS policies mirroring `fp_items` (read via practice.take, write via items.manage) if missing (check `pg_policies` first).
6. **Policy rows** (`platform_policies`, published, house style): `onemark.paper.question_count.tn_hsc_english = 20` (removes Lane W's hard-coded fallback), `onemark.live.auto_close_after_minutes = 30` [risky number], `onemark.live.grace_seconds = 15` (Lane V's literal), `onemark.results.min_learners_for_item_stats = 5` (distractor stats hidden below this — small cohorts identify individuals).
8. **Board-paper hits (Lane Q item 4).** `onemark_board_paper_hits (id uuid PK DEFAULT gen_random_uuid(), exam_definition_id uuid NOT NULL REFERENCES exam_definitions, exam_year int NOT NULL, sitting text, item_id uuid NOT NULL REFERENCES fp_items ON DELETE CASCADE, match_kind text NOT NULL CHECK (match_kind IN ('exact','near')), board_qno int, note text, noted_by uuid REFERENCES profiles, noted_at timestamptz NOT NULL DEFAULT now())`, UNIQUE `(item_id, exam_year, COALESCE(sitting,''))` (expression index), index `(exam_definition_id, exam_year)`. RLS: read for signed-in holders of `foundation.practice.take` OR `foundation.items.manage`; write `foundation.items.manage`; `REVOKE ALL … FROM anon, PUBLIC`. Append-only by design — a wrong tick is deleted by its author, never edited.
9. **Vault draw by source (Lane Q item 3).** New overload `fn_onemark_vault_draw(p_student_id uuid, p_exam_definition_id uuid, p_count int, p_source_keys text[])` — same body and gates as the live 3-argument version plus `AND (p_source_keys IS NULL OR i.source_key = ANY (p_source_keys))`; the 3-argument version stays (Lane V calls it). Same grants (`REVOKE … FROM anon, PUBLIC; GRANT … TO authenticated`).
10. **Source analytics RPC (Lane Q item 5).** `fn_onemark_source_analytics(p_exam_definition_id uuid, p_exam_year int DEFAULT NULL) RETURNS jsonb` — SECURITY DEFINER, `search_path = public`; caller must hold `foundation.assessments.manage` OR an active `school_jkkn_owners` row (the same scope Director decision 1 settles). Per `source_key` (NULL bucket kept, labelled unrecorded): `items_total`, `items_active`, `times_served`, `times_correct`, `hits_exact`, `hits_near` (for `p_exam_year`, or all years when NULL), `hit_rate` (hits ÷ items_active, NULL when 0 active), and `lift` per Lane Q item 5 from `fp_attempts` (`mode='live'` scores vs each learner's practice share by source, read from practice attempts' `config.source_keys` and the served items' `source_key`) — `lift` and its learner counts are NULL with a `reason` when fewer than `onemark.results.min_learners_for_item_stats` learners qualify. Never returns a stem, an option or an answer.
11. **Retire, do not delete.** `BEFORE DELETE ON onemark_item_sources` trigger raising `check_violation` ('retire a source with is_active = false; deleting would blank provenance on every linked question') — the second wall behind Lane Q's 405.
7. Header: rollback recipe for every step; step-12 DO block asserting all objects exist (items 1–11) and anon is locked. [risky]: the 30-minute auto-close window; results visibility scope (see Director decisions).

## Lane A — `feat/onemark-w3-results` — Senior Learner results and analytics (PRD Phase 3) — CONTRACT-DEPENDS ON S3
Own: `app/(routes)/foundation/onemark/results/**`, `app/api/foundation/onemark/results/**`, `lib/services/onemark/results-*.ts`, `__tests__/onemark/results-*.test.ts`.
1. `/foundation/onemark/results` — list of the caller's papers (`fp_assessments.kind='mock'` they created or whose cohort school they manage), with sat/total, average score, published/closed state. Gate `foundation.assessments.manage`; no rows → explicit empty state, never a redirect.
2. `/foundation/onemark/results/[assessmentId]` — the cohort sheet from `fn_onemark_cohort_results`: score list (decision 17: digital sittings flagged), per-unit and per-tag heat strip, item table with p-value and top distractor (hidden when learners < `onemark.results.min_learners_for_item_stats`). Export: CSV of the score list (`/api/foundation/onemark/results/[id]/export`) — no answers, no explanations.
3. `/foundation/onemark/results/learner/[studentId]` — the learner report from `fn_onemark_learner_report` (also linked from `/foundation/students/[id]`, which Lane N wires). A learner may open their OWN report at `/foundation/onemark/practice` → "My progress" (Lane L renders that card; Lane A exposes the API only).
4. Charts: load the `dataviz` skill before drawing; one scale per chart; theme tokens; no chart library unless it carries weight.
5. Gates: scoped tsc, vitest on the service (fixtures for 0 / 1 / 5 / 40 learners), `npm run build`, mirrors. [risky]: what a principal (owner row, no `assessments.manage`) may see — see Director decisions.

## Lane T — `feat/onemark-w3-tamil-ui` — per-person Tamil / English interface (decision 5) — CONTRACT-DEPENDS ON S3
Own: `lib/onemark/i18n/**` (new), `app/(routes)/foundation/onemark/practice/_components/**` string extraction ONLY (no logic), `app/(routes)/foundation/onemark/paper/_components/**` string extraction ONLY, `app/(routes)/foundation/onemark/review/_components/**` string extraction ONLY, `app/api/foundation/onemark/prefs/route.ts`, `__tests__/onemark/i18n-*.test.ts`.
1. Mechanism (nothing exists in MyJKKN today — verified 2026-09-06): `lib/onemark/i18n/strings.{en,ta}.ts` typed dictionaries + `useOneMarkT()` hook reading `onemark_user_prefs.ui_locale` (session client, cached in React state; default `en`) + a toggle (EN | தமிழ்) in the OneMark hub header and the practice runner. Persist via `PUT /api/foundation/onemark/prefs {ui_locale}`.
2. Scope: **learner surfaces first** (hub cards, practice/timed/live runner, vault panel, sitting review, empty states, error messages), then the paper wizard and review queue. Sidebar labels and the guide are Lane N's, NOT yours — leave them English.
3. Every Tamil string > 5 words is `[TAMIL_TBD: …]` (rule #24); ≤ 5-word strings may be written but the PR body lists ALL Tamil strings in a review sheet for a native reviewer (Lane 0 item 2). English is the fallback for any missing key — a missing Tamil key must never render an empty control.
4. Question CONTENT is already bilingual (`stem_ta`, `options_ta`); do not touch the content path.
5. Gates: scoped tsc, a test that every key in `strings.en` exists in `strings.ta` (placeholders allowed), `npm run build`, terminology (English strings only). [risky]: placeholder count at ship.

## Lane D — `feat/onemark-w3-diagrams` — images in questions (Physics circuits, ray diagrams) — CONTRACT-DEPENDS ON S3
Own: `lib/onemark/pdf/**` (image block only), `app/(routes)/foundation/onemark/review/_components/asset-*.tsx` (new), `app/(routes)/foundation/_components/item-author-dialog.tsx` is OFF-LIMITS (Wave 1 file) — add an "Attach diagram" step INSIDE the review queue's draft card instead, `app/api/foundation/onemark/assets/**`, `__tests__/onemark/assets-*.test.ts`.
1. Upload: PNG/JPEG/SVG ≤ 2 MB to `onemark-question-assets/<item_id>/<uuid>.<ext>` via a route that checks `foundation.items.manage`, strips EXIF, and writes `onemark_question_assets` (alt_text REQUIRED — the PDF prints it for screen readers' sake and the review queue refuses to approve an item whose image lacks it).
2. Render: practice runner (Lane L owns the runner — you export `<QuestionAsset>` from `lib/onemark/assets/render.tsx` and Lane L drops it in), PDF (`lib/onemark/pdf/document.ts`: image between stem and options, max width = text column, Tamil block shares the image), answer key unchanged.
3. Signed URLs (60 s) for learner reads; never public.
4. Gates: scoped tsc, vitest, PDF fixture with one image per subject rendered to PNG and EYEBALLED (rule #25), `npm run build`. [risky]: SVG sanitisation (reject scripts/foreignObject) — say what you did.

## Lane L — `feat/onemark-w3-live-ops` — live-sitting operations + served-set + learner progress card — CONTRACT-DEPENDS ON S3
Own: `lib/services/onemark/attempt-server.ts`, `app/api/foundation/onemark/attempts/**`, `app/api/cron/onemark-live-autoclose/route.ts` (new) + its `vercel.json` cron line, `app/(routes)/foundation/onemark/practice/page.tsx` + `_components/**` (logic only; Lane T owns string extraction there — coordinate by touching different lines, never the same string), `__tests__/foundation/onemark-attempt-routes.test.ts`.
1. Persist `served_item_ids` on POST /attempts (S3 item 1); keep the HMAC token for ONE release as belt-and-braces, then remove it in a follow-up once S3 is live and proven — say which state the PR ships.
2. Cron `/api/cron/onemark-live-autoclose?secret=${CRON_SECRET}` every 10 minutes → `fn_onemark_close_abandoned_live()` with the service client; log the count; idiom of `app/api/cron/onemark-item-drafts`.
3. "My progress" card on the learner home (`/foundation/onemark/practice`): from Lane A's learner-report API — score trend of last 10 sittings, vault due count, weakest unit. Never shows answers.
4. Drop `<QuestionAsset>` (Lane D) into the runner behind a null-check so the runner builds before D merges.
5. Gates: scoped tsc, vitest (auto-close idempotency with a fake clock; served-set refusal), `npm run build`, mirrors. [risky]: 15-second grace vs policy row; token retirement timing.

## Lane Q — `feat/onemark-w3-sources` — question sources: manage the list, pick by source, judge each source after the board exam — CONTRACT-DEPENDS ON S3 (items 8–10)
Director rulings 2026-09-06 09:2x IST (this pane, AskUserQuestion): (a) "this source worked" means BOTH — its questions appeared in the real board paper AND learners who practised it improved; (b) any question author (`foundation.items.manage`) may add, edit and retire a source; (c) learners may pick sources in practice and vault review, not only Senior Learners building papers.
Already live — reuse, do not rebuild: `onemark_item_sources` (key, label, description, is_system, is_active, sort_order + the config-table mixin), 5 seeded rows; FK `fp_items.source_key → onemark_item_sources(key) ON DELETE SET NULL`; write policy `onemark_item_sources_write` = `is_super_admin() OR user_has_permission('foundation.items.manage')` — ruling (b) needs NO policy change; the paper wizard's Step 2 source filter (`step-filters.tsx`, `paper-service.ts` `source_keys`). Read live 2026-09-06: the bank holds 1 active item and its `source_key` is NULL — every analytics screen below must render an honest empty state first.
Own: `app/(routes)/foundation/onemark/sources/**` (new), `app/api/foundation/onemark/sources/**` (new), `app/(routes)/foundation/onemark/results/sources/**` + `app/api/foundation/onemark/results/sources/**` (new; Lane A owns the rest of `results/`), `lib/services/onemark/sources-*.ts`, `__tests__/onemark/sources-*.test.ts`. Two files outside: the source `<Select>` in `app/(routes)/foundation/onemark/review/_components/draft-card.tsx` (replace the `SOURCE_LABELS` literal with the table) and `source_keys?: string[]` on POST `/api/foundation/onemark/attempts` — Lane L owns that route, so Q hands L the patch (or lands it after L merges); never two open PRs on one file.
1. `/foundation/onemark/sources` — the list with active/total item counts per source; add (key = slug of the label, immutable after create), edit label / description / sort order, retire and re-activate (`is_active`). NO hard delete anywhere — the FK would blank every linked question's provenance; the API answers DELETE with 405 and S3 adds a BEFORE DELETE trigger as the second wall. `is_system` rows: key and flag fixed, label editable, cannot be retired. A retired source disappears from every picker (review card, wizard, learner chips) but stays on its questions and shows greyed in analytics. Gate `foundation.items.manage`; explicit empty and forbidden states (rule 27).
2. Source picker wherever a question is born: the review-queue approve card reads the table (item above), the AI draft run form defaults to `internal`, `scripts/onemark/ingest-board-paper.ts` keeps `past_board_exam`. No literal source lists remain in code — grep proves it in the PR body.
3. Learner pick (ruling c): source chips on the practice start card and the vault-review panel (default = all, retired hidden). POST `/attempts` takes `source_keys`, applies `.in('source_key', keys)` to the practice draw and passes them to S3's `fn_onemark_vault_draw` overload; the chosen keys are persisted on `fp_attempts.config.source_keys` so analytics knows what was practised. Empty list = all, as the wizard does.
4. Board-paper tagging (ruling a, first half): `/foundation/onemark/sources/board-paper` — pick exam + year + sitting, search the bank by stem, tick "appeared" as exact or near, record the board question number → S3 `onemark_board_paper_hits`. Gate `foundation.items.manage`. A once-a-year job after each real exam; Lane 0 owns the calendar entry.
5. Source analytics (ruling a, both halves): `/foundation/onemark/results/sources` from S3 `fn_onemark_source_analytics`. Per source: active items · times served · accuracy · board hits (exact / near) · hit rate = hits ÷ active items for the chosen exam year · learning lift = mean live-paper score of learners whose practice share on this source is ABOVE the cohort median minus those below, hidden under `onemark.results.min_learners_for_item_stats`. One chart (load `dataviz` first), one table, and plain-English footnotes saying what each number can and cannot claim: hit rate is per past exam year; lift is a correlation, not a cause. The `NULL` source bucket is shown as "source not recorded", never dropped.
6. Gates: scoped tsc, vitest on the service (retire hides from pickers; slug immutability; DELETE → 405; analytics fixtures for 0 / 1 / 5 / 40 learners; hit rate with 0 hits and with a retired source), `npm run build`, terminology, mirrors. [risky]: the median-split lift definition is the builder's proposal, not a ruling; keys immutable after create; system rows non-retirable.

## Lane U — `feat/onemark-w3-units` — the school units screen (subject-first), and school units leave the coaching grid
Director ruling 2026-09-06 09:4x IST: "the units are not organized as per subject wise. It needs to be a greater user interface and user experience" → build a OneMark units screen, grouped by subject, and stop showing school units on the government-coaching grid.
MEASURED ON PRODUCTION 2026-09-06 (persona harness, `test.superadmin@jkkn.ac.in`, screenshots `.screenshots/mint-units-master.png` + `mint-topic-map.png`):
- **The data is already right; only the screens are wrong.** `exam_topic_map.sort_order` is per exam and clean — Physics 1–11, English 1–6 plus 99 for "Grammar (General)". **No data migration is needed.** What interleaves the subjects is `cdc_exam_syllabus_topics.sort_order`, the topics table's own global column, where both subjects start at 1; every flat listing ordered by it zips Electrostatics against Two Gentlemen of Verona.
- **`/cdc/admin/exam-topic-map` shows all 18 school units as rows in a grid whose columns are TNPSC / RRB / IBPS / SBI / SSC.** Every checkbox on those rows is empty and always will be: the two `tn_hsc_*` exams are not columns there. 80 coaching mappings share the grid with 18 rows that can never be ticked.
- **`/cdc/admin/exam-syllabus-topics`, the only screen that edits a unit, does not render** — a server page handing a `render` function to the client `MasterTablePage`. Fixed separately in PR #3312 (four pages, same cause); Lane U assumes that is merged and does not touch those files.
Own: `app/(routes)/foundation/onemark/units/**` (new), `app/api/foundation/onemark/units/**` (new), `lib/services/onemark/units-*.ts`, `__tests__/onemark/units-*.test.ts`. One file outside: `app/(routes)/cdc/admin/exam-topic-map/page.tsx` (item 2) — coordinate with whoever holds `/cdc/admin`; never two open PRs on one file.
1. `/foundation/onemark/units` — **one section per subject, never one flat list**, each ordered by `exam_topic_map.sort_order`, each row showing its unit list position and item counts (bank total · active · awaiting review). Add a unit = one action that writes the `cdc_exam_syllabus_topics` row AND its `exam_topic_map` row at the next free position for that subject (never leave a topic unmapped — an unmapped topic is invisible to the wizard and the drafter). Rename, re-order within a subject (writes `exam_topic_map.sort_order` only, never the shared global column), retire (`is_active=false`). **No hard delete**: `fp_items.topic_id` points here and a delete would orphan every question of that unit; the API answers DELETE with 405. Gate `foundation.items.manage`; explicit empty and forbidden states (rule 27).
2. School units leave the coaching grid: `/cdc/admin/exam-topic-map` lists only exams whose `config_key` is not a OneMark subject (or gains an exam-family filter that defaults to coaching). The 80 coaching mappings are untouched; the 18 school rows are reachable at the new screen instead. Say in the PR body which of the two shapes shipped.
3. The wizard, the drafting form and the review queue keep their own subject-scoped pickers (already correct — the wizard picks the subject in step 1, the review queue has a tab per subject); Lane U changes none of them, and the PR body must show a grep proving no new unit list is ordered by the topics table's global `sort_order`.
4. Tamil unit names carry `[TAMIL_TBD]` where a native reviewer has not signed off (rule #24, and Wave 1 flagged every seeded row "needs native review"); the screen shows the English name with the Tamil one beneath when present, and marks the unreviewed ones so the review can actually be run from this screen.
5. Gates: scoped tsc, vitest on the service (add-unit writes both rows; re-order touches only the map; DELETE → 405; a retired unit disappears from pickers but keeps its questions), `npm run build`, terminology, mirrors. Persona snapshot on the new page as a Senior Learner AND as a super admin, screenshots in the PR. [risky]: item 2 changes a screen another module owns; the "next free position" rule when a subject has a 99-style sentinel row.

## Lane G — `feat/onemark-w3-ai-request` — the door a Senior Learner uses to ask for AI questions
Director ruling 2026-09-06 10:0x IST, after "Where is [the] AI button on the UI for senior learners": there is none, and one is owed — spec it for Wave 3.
MEASURED 2026-09-06: `POST /api/foundation/onemark/draft` (Lane I) is deployed and answers its gate correctly, but **nothing in the app calls it** — a repo-wide search for the path returns the route, two service files and comments, no caller. The only surface that can start ANY AI job by hand is the generic Run card at `/admin/ai-models`, whose sidebar key is `super_admin`. So decision 7 ("AI drafts, one subject Senior Learner approves") has a working approval half and no drafting door: a Senior Learner cannot request questions at all, and a super admin has to do it from an admin console. The review queue's own empty state already promises the feature ("Drafts arrive from an ingested past paper or an AI drafting request"), so the copy is ahead of the UI.
Also fixed before this lane starts (do not re-fix): the job type asked its runner for a table write it does not implement, so every request failed in ~60 ms — `20260918140000` (PR #3311) repointed it at `job.result`, applied 2026-09-06; the runner now writes model text to `ai_jobs.result` and the Lane J cron files drafts into `fp_items`.
Own: `app/(routes)/foundation/onemark/review/_components/request-drafts-*.tsx` (new), `lib/services/onemark/draft-request.ts`, `hooks/onemark/use-draft-request.ts`, `__tests__/onemark/draft-request-*.test.ts`. Do NOT touch `app/api/foundation/onemark/draft/route.ts` — it is complete and validated (schema fields, tag ownership, count 1–20, JABT level, 503 when the contract row is missing); this lane is the client for it.
1. A **"Request AI questions"** panel on `/foundation/onemark/review`, above the queue it fills — the screen a subject Senior Learner already opens to approve drafts, so asking and approving live together. Fields come from the job type's `input_schema` (subject, unit — nullable for the English grammar-general tag set, category tags, how many, JABT level). Plain words, not job-queue words: a person asks for questions, not for a "job".
2. Honest state, no spinner theatre: after POST, show the request with its queue position and poll `GET /api/ai-jobs/status?id=`. Say plainly that drafts appear in the queue below **after the collect pass** (`:09` and `:39` past the hour), so nobody watches a screen expecting instant rows. Terminal states: filed (with the count inserted and the count rejected), errored (with the reason), still waiting.
3. Caps are visible BEFORE the click, never discovered by a refusal: 5 requests per person per day and ₹5,000 per month across the estate (`ai_job_types` row). Show today's remaining count for the caller; when it is 0, disable the button and say when it resets.
4. Every drafted question arrives inactive and unapproved. The panel says so in one line — the drafting request is not an approval, and decision 7's single-teacher tick still stands between a draft and a learner.
5. Gates: scoped tsc, vitest (cap exhaustion, a 503 contract-pending response, a rejected-items summary, poll terminal states), `npm run build`, terminology, mirrors. Persona snapshot as a Senior Learner holding `foundation.items.manage` AND as one without it (the panel must be absent, not broken). [risky]: whether the daily cap is read live or assumed; whether English's unit-less tag sets need the unit field hidden rather than optional.

## Lane N — `feat/onemark-w3-wiring` — navigation, guide, hub door — LAST
Own: `lib/sidebarMenuLink.ts`, `lib/foundation/guide/**`, `lib/navigation/modules.ts` (mobile tabs), `app/(routes)/foundation/students/[id]/page.tsx` (one link only).
0. Lane G needs no new door — its panel lives on `/foundation/onemark/review`, already in the sidebar.
0. Lane U door: `/foundation/onemark/units` (key `foundation.items.manage`) joins the OneMark sidebar and the guide.
0. Lane Q doors: `/foundation/onemark/sources` + `/foundation/onemark/sources/board-paper` (key `foundation.items.manage`) and `/foundation/onemark/results/sources` (key `foundation.assessments.manage`) go into `MENU_PERMISSIONS`, the sidebar entries and the guide; the learner guide gains one line on picking a source.
1. Sidebar: "Results" row for `foundation.assessments.manage` (`/foundation/onemark/results`); a door to the hub `/foundation/onemark` WITHOUT revealing the operator accordion to learners (Wave 2 R proved a practice.take-keyed child leaks it — use a flat learner row or the mobile module tabs; if Academic would hit 15 top-level rows, say so and stop).
2. Guide: results and Tamil-toggle steps; labels copied VERBATIM from the built screens (grep the merged main, not the spec — Wave 2 R lost a round to "Practise").
3. Link from `/foundation/students/[id]` to the learner report.
4. Gates: `check:sidebar`, guide-smoke, nav-config, visibility tests in `__tests__/lib/`, terminology. Un-draft only after A, L, T are on main.

## Lane 0 — operations (people, not code; the Director owns the taps)
1. **Fill the bank to 300 per subject** (decision 8): ingest past board papers (`scripts/onemark/ingest-board-paper.ts`, Lane I) + "draft now" per unit (Lane J, ₹5,000/month cap); one Senior Learner per subject ticks each item in `/foundation/onemark/review`. Track the count weekly; nothing else in Wave 3 matters until this moves.
2. **Native Tamil review**: (a) one printed paper per subject (rule #25), (b) Lane T's string sheet, (c) unit names. Reviewer named by the Director.
3. **Matric HSS Senior Learners**: add institution `e04b8a7f-1445-4ef1-92e9-bde3d32b1f44` to `onemark.provision.institution_ids` (one policy update) so its `faculty` / `hod` profiles self-provision on first sign-in — Director decision pending since 2026-09-05.
4. **Real-user click-through on www.jkkn.ai** of every Wave 2 mode (passkey) before any learner sits a paper.
5. **First live sitting** with one cohort; the auto-close cron and results sheet get their first real data.

## Director decisions needed BEFORE launch (ask in one round, plain English)
1. Results visibility: may a school principal (owner row, no `assessments.manage`) see every cohort sheet of his school? [recommend: yes via owner row]
2. May a learner see their own item-level wrong answers after a LIVE paper closes, or only the score and weak units? [PRD: score + explanation after finalize; recommend: full review after close_at]
3. Auto-close window after a live paper's close time: 30 minutes? [S3 policy row]
4. Diagram uploads: allowed for every `items.manage` holder, or approver-only? [recommend: every author, approver must still tick]
5. Tamil interface scope for this wave: learner surfaces only, or also the wizard and review queue? [recommend: learner first; wizard/review next wave]
6. Matric HSS auto-provisioning (Lane 0 item 3): yes / no.

### Decided 2026-09-06 09:2x IST — question sources (Director, AskUserQuestion, three taps)
1. "A source worked" = BOTH measures: its questions appeared in the real board paper (tagged after exam day) AND learners who practised it improved (lift). Both shown side by side; neither alone.
2. Any question author (`foundation.items.manage`) may add, edit and retire a source — the live write policy already says so; retire hides, nothing deletes.
3. Learners may pick sources in practice and vault review; Senior Learners keep the wizard filter.

### Decided 2026-09-06 09:4x IST — units are organised subject-first (Director)
"the units are not organized as per subject wise. It needs to be a greater user interface and user experience." → Lane U. The units data needs no migration: `exam_topic_map.sort_order` is already per subject and correct; the shared topics table's global `sort_order` is what interleaves them, and no OneMark screen should order by it again.

### Decided 2026-09-06 10:0x IST — Senior Learners get an AI-request door (Director)
"Where is [the] AI button on the UI for senior learners" — there is none. `POST /api/foundation/onemark/draft` has no caller anywhere in the app, and the only manual AI-run surface (`/admin/ai-models`) is super-admin only. Ruling: build it, spec it for Wave 3 → Lane G, as a panel on the review queue.

## Deferred (not Wave 3, by ruling)
Offline Android app (decision 1 — only if no-signal proves real); subjects beyond Physics and English; Parts II–IV; parent/guardian view (PRD minors-data rule keeps it out until asked).
