# Cohort Core — Shared Cohort Engine (build plan + progress)

> **Loop state file.** This is the durable source of truth for the `/loop` autonomous build.
> Each iteration: build the next unchecked item → scoped-verify → commit → tick the box here.
> Survives `/clear`. Branch: `feat/cohort-core` (off `jicate/main`). Worktree: `.claude/worktrees/cohort-core`.

## Guardrails (non-negotiable)
- **PR-staged only.** Never auto-apply migrations to prod DB, never fire a deploy inside the loop. All work lands in the branch.
- **Definition of "green"** = `tsc --noEmit` shows **no NEW errors in files this branch touches** (repo has ~1000 pre-existing) + `eslint` clean on touched files + `npm run build` route/sidebar/reachability gates pass. No repo-wide typecheck-zero (structurally unmeetable).
- Every new SECURITY DEFINER RPC: `REVOKE EXECUTE FROM anon, PUBLIC; GRANT TO authenticated`.
- RLS on every table; `institution_id` scoping via `role_has_institution_access()`; `is_super_admin() OR is_admin()` first in every policy.

## Locked program rules (from interview 2026-06-22) — these live in the SHARED lifecycle engine
1. Paid user = **still paying this month** (churn drops count; MRR-based)
2. Proof = team uploads proof, **mentor spot-checks** (esp. near target)
3. Leaderboard ranks by **mentor-verified** members only
4. Reaching target → **mentor/coordinator approves** before graduation
5. Drop below target → **30-day grace period**
6. Pivot → **mentor decides** if progress resets
7. Missed deadline → **mentor decides** (warn/extend/drop)
8. One person, two cohorts → **only with mentor approval**
9. Mentor unresponsive → **escalate to coordinator after 3 business days**
10. No team/solo → **help-match** into a cohort
11. Goes quiet → **nudge, then pause** (never delete)
12. Team size → **2–5**
13. Structure → **repeating cohorts, fixed length**
14. Round ends under target → **rolls into next round** (keeps progress)

Rules 1–4 are SF100-startup-specific (extension layer). Rules 5–14 are generic cohort lifecycle (shared spine).

## Cross-domain decisions (interview 2 — 2026-07-05) — how the SHARED engine behaves across all 4 programs
- **D1 · Admin visibility** — KEEP platform-standard (`is_admin()` sees all institutions). Phase-1 RLS is correct as-is; no change needed.
- **D2 · Rule application** — PER-PROGRAM SWITCHES. Each cohort toggles which shared lifecycle rules it uses via `cohorts.config` (jsonb). Define a rules-config shape with a sensible default per `kind`.
- **D3 · SF100 migration (Phase 2)** — COPY-OVER + KEEP OLD AS BACKUP. Backfill `sf100_programs`(1) + `sf100_enrollments`(18) into `cohorts`/`cohort_memberships`; leave `sf100_*` tables intact until sign-off. Fully reversible. **⚠️ SUPERSEDED 2026-07-05 by the FULL MIGRATION (Phase 2.3 below):** the "parallel copy, no cutover" stance is retired — `cohorts`/`cohort_memberships` are now the CANONICAL "which program + roster + membership status" store and the SF100 service/routes READ from them (with legacy fallback). `sf100_enrollments` is DEMOTED to an SF100 per-team **extension** linked by `cohort_membership_id`. Only the **DROPs** remain deferred (a later follow-up after live-verify + sign-off) — so D3's "keep old as backup" survives narrowly as "keep the tables until the drop", not as "keep them canonical". The originally-planned go-live sync interview is thus **moot**: cutover is code-level (this PR), not a separate data event.
- **D4 · Cross-program membership** — ALLOWED. A person may be active in multiple DIFFERENT programs at once. (Same-program dual membership still needs mentor approval — rule 8.)
- **D5 · Foundations completion (Phase 3)** — MENTOR SIGN-OFF marks a student complete.
- **D6 · Trainer development (Phase 5)** — fold in LAST, after Phases 2–4 prove the engine; re-decide fit-vs-standalone at that point.
- **D7 · Round close/archive** — members AUTO-complete/archive with the round; their record is KEPT as history (cohort-service cascade).
- **D8 · Member transfer** — ALLOWED between cohorts with history preserved (enables roll-into-next-round, rule 14); coordinator-gated in the UI. Add `transferMembership` to cohort-service.
- **D9 · Member identity = ALWAYS profile-linked** (2026-07-06) — every cohort member is an EXISTING MyJKKN user, so identity MUST resolve to a real profile (`profiles`/`learners_profiles`). **No free-text-only members.** `member_ref` (shared engine) and `profile_id`/`learner_id` (SF100 roster) are the source of the name; any `full_name`/`email` column is a DISPLAY cache derived from the profile, never authored directly. Enforcement: (a) member-add UI = existing-user **picker only**, no free-text field — ✅ **SHIPPED Phase 6** (`components/cohort-core/member-picker.tsx`); (b) `cohort-service` add/transfer validates `member_ref` resolves to a real identity for `student`/`learner`/`staff` types — ✅ **SHIPPED Phase 6** (`CohortService.assertMemberIdentity`, wired into `createMembership`+`transferMembership`); (c) DB guard on `sf100_roster_changes` — `CHECK (profile_id IS NOT NULL OR learner_id IS NOT NULL)` (safe: 0 rows today) — ✅ **IMPLEMENTED in PR #1814** (migration `20260731070000_sf100_roster_profile_required.sql`, PR-staged/not applied). Made safe by first fixing the sole write path: `SF100Service.requestRosterChange` now resolves the member's identity (explicit DTO → matching `event_team_members` row → directory lookup by email against `profiles`/`learners_profiles`) and rejects with a 400 when nothing resolves, so no code path can insert a free-text-only (NULL/NULL) row.

## Architecture — thin shared spine + domain extensions
- `cohorts` — id, kind ('sf100'|'foundations'|'cdc'|'trainer'), name, institution_id, owner_id, opens_at, closes_at, hard_deadline, status, academic_year, config jsonb, archived_at/by
- `cohort_memberships` — id, cohort_id, member_type ('team'|'student'|'learner'|'staff'), member_ref, status, role, joined_at/by, config jsonb
- `cohort_status_events` — id, cohort_id, membership_id, event_type, from_status, to_status, actor_id, reason, metadata, created_at (powers nudge→pause, escalation, grace-period, audit)
- Shared status enum: `draft → enrolling → active → completed → archived` (cohort); `invited → enrolled → active → graduated | removed | paused` (membership)
- Shared service `lib/services/cohort-core/` + shared hooks `hooks/cohort-core/` + shared UI `components/cohort-core/` (list, roster table, status badge, lifecycle machine)
- Each domain keeps its extension tables (SF100: paid_users/exercises/pivots/check_ins/interviews; CDC: attendance/cert; Trainer: earnings/levels/assignments) and registers its container into `cohorts`.

## State of the world (verified 2026-07-05)
- No `cohorts`/`cohort_memberships`/`cohort_status_events` yet (clean slate).
- SF100: 1 program, 18 enrollments, 0 paid_users. Foundations/CDC/Trainer containers = 0 rows (empty scaffolds).

---

## PHASE 1 — Core spine (additive, 🟢 no live data touched) — ✅ DONE → PR #1797
- [x] 1.1 Migration: `cohorts`, `cohort_memberships`, `cohort_status_events` tables + enums + indexes + RLS (institution-scoped) + updated_at triggers
- [x] 1.2 Update `supabase/setup/01_tables.sql`, `03_policies.sql`, `04_triggers.sql`, `SQL_FILE_INDEX.md`
- [x] 1.3 Shared TS types `lib/types/cohort-core.ts` (Cohort, CohortMembership, statuses)
- [x] 1.4 Shared service `lib/services/cohort-core/cohort-service.ts` (CRUD + status transitions)
- [x] 1.5 Lifecycle engine `lib/services/cohort-core/lifecycle.ts` (nudge→pause, escalation-after-3d, grace-period) — pure functions, unit-testable
- [x] 1.6 React Query hooks `hooks/cohort-core/index.ts`
- [x] 1.7 Scoped verify (tsc/eslint on touched files) + commit — tsc 0 / eslint 0; 11-agent adversarial review = 0 confirmed critical/high

## PHASE 2 — SF100 onto the core as reference (🟡 live data: 19 rows) — approach = FULL MIGRATION (re-key/repoint only; NO drops) — SUPERSEDES D3 copy+keep
- [x] 2.0 Engine: per-program rule-config shape on `cohorts.config` + defaults per kind (D2); add `transferMembership` (D8) and round-close cascade that auto-archives members (D7) to cohort-service + lifecycle
- [x] 2.1 Map `sf100_programs` → register into `cohorts` (kind='sf100'); `sf100_enrollments` → `cohort_memberships`
- [x] 2.2 Backfill migration for the 1 program + 18 enrollments (idempotent, COPY only — leave sf100_* intact as backup per D3)
- [x] 2.3 **FULL MIGRATION (PR-staged, NOT applied):** demote `sf100_enrollments` to an SF100 per-team extension via ONE nullable link column `cohort_membership_id` → `cohort_memberships(id)` ON DELETE SET NULL (migration `20260731060000_sf100_demote_to_extension.sql`, idempotent, drops-nothing); REPOINT the 6 SF100 data-layer files so "which program + roster + membership status" come from `cohorts`/`cohort_memberships` while SF100 per-team fields stay on the extension (joined by `member_ref`). Legacy `program_id`/`sf100_programs`-column fallbacks on every repointed read → safe to deploy before the migration is applied and no RLS regression. `enrollTeam` gets the write-side twin (mirrors a `cohort_memberships` row + sets the link). Program CRUD (`getProgram`/`listPrograms`/`createProgram`/`updateProgram`) intentionally stays on the still-live `sf100_programs` (FK target of `sf100_enrollments.program_id`; drops deferred). Stall SUB-STATES (`runStallCheck` escalation, `submitCheckIn` reset, `getStalledTeams`) keep reading `sf100_enrollments.status` (the 6-value ENUM — folding to membership.status would break stall detection, gotcha #3).
- [ ] 2.4 Scoped verify + commit (tsc/eslint on touched files; PR via ship worktree)

### Phase 2.3 repoint map (what reads what, post-migration)
| Read | Program/roster source | SF100 fields source | Notes |
|---|---|---|---|
| `listEnrollments`, `getVerificationQueue`, `bulkAutoAdvance`, `runStallCheck` roster, `getPublicLeaderboard`, `getPublicStats`, `getPhaseFunnel`, `getStalledTeams`, `exportProgramCSV` | `cohorts`→`cohort_memberships` (member_ref), legacy `program_id` fallback | `sf100_enrollments` (`.in('id', member_refs)`) | existing `sf100_enrollments.status` filter kept verbatim (authoritative sub-state) |
| `checkAutoAdvance` `max_internal_user_pct` · `runStallCheck` thresholds · `notifyEvaluationRoundOpen` `source_event_id`/`name` | `cohorts.config` / `cohorts.name`, fallback to `sf100_programs` columns | — | program config now lives in `cohorts.config` (D2) |
| `getMyEnrollment` | unchanged (`event_team_members`→registration_ids→`sf100_enrollments`) | `sf100_enrollments` | per-user lookup, not a program-roster read |
| pipeline `getSummary`/`getTeamsAtStage` SF100 | **stays on extension** (documented) | `sf100_enrollments` | folding to membership.status would change the active-only count |
| `roster-changes/pending` | unchanged | `sf100_enrollments` | `sf100_roster_changes.status` is a request-workflow status, not lifecycle |

## PHASE 3 — Foundations onto the core (🟢 empty) — ✅ BUILT (PR-staged, NOT applied)
- [x] 3.1 Map `ss_foundations_cohorts/enrollments` → cohort-core (kind='foundations'); completion = MENTOR SIGN-OFF (D5) → graduates the membership
- [x] 3.2 Service/hooks + scoped verify + commit

### Phase 3 approach (mirrors SF100 Phase 2.3 — ADDITIVE fold, NO drops)
- **Migration** `20260731080000_foundations_demote_to_cohort_core.sql` (idempotent, drops-nothing): ONE nullable link column `ss_foundations_enrollments.cohort_membership_id` → `cohort_memberships(id)` ON DELETE SET NULL (pg_constraint-guarded) + reverse index; **D9 CHECK** `student_id IS NOT NULL`; **self-enrol RLS carve-out** `cohort_memberships_foundations_self_insert`; empty-table NOTICE (0 rows → no populate, no abort). Mirrored into `setup/01_tables.sql` (ALTER) + `setup/03_policies.sql` (carve-out) + `SQL_FILE_INDEX.md`.
- **`ss_foundations_cohorts` stays authoritative (gotcha #2):** NOT dropped/re-keyed — `ss_foundations_worksheets.cohort_id` + `ss_foundations_responses.cohort_id` FK it and the listWorksheets override keys off it. `cohorts` (kind='foundations') is an **additive spine MIRROR** (mapped `config->>'ss_foundations_cohort_id'`) for roster+lifecycle, minted forward in the service. So container reads (`listCohorts`/`getCohort`/`getMyEnrollments`) + roster (`listEnrollments`) stay on `ss_foundations_*` → **UI signatures unchanged**.
- **Service** `foundations-service.ts`: `createCohort` mints the mirror (best-effort); `enroll` gains a **D9 profile-resolve guard (clean 400)** + best-effort membership twin (`member_type='student'`, `member_ref=student_id`) + back-link + 'enrolled' event; `withdraw` also flips the membership → 'removed'; new **`signOffGraduation`** (D5): assert global completion ≥ threshold → enrollment `completed` → best-effort membership → 'graduated' + event + notify.
- **Routes:** facilitator-add + self-enrol wrap the D9 400; new `POST …/foundations/enrollments/[id]/complete` (gated `startup_studio.foundations.review`).
- **Gotcha #1 (self-enrol under RLS):** the self-enrol POST runs AS THE STUDENT (never service-role); the carve-out lets the `member_type='student'` self-insert succeed while graduation stays mentor-gated. **D5/D9 write via direct `this.supabase` (server-side), NOT `CohortService` (client-only browser client).** All spine writes are BEST-EFFORT — the extension roster is authoritative, so a lagging/RLS-denied mirror can never drop a real enrollee.

## PHASE 4 — CDC training onto the core (🟢 empty)
- [x] 4.1 Map `cdc_training_programmes` + `cdc_training_enrollments` → cohort-core (kind='cdc'); keep attendance/cert extension — migration `20260731090000_cdc_training_demote_to_cohort_core.sql` (link column + FK ON DELETE SET NULL + reverse index + L3 partial unique index on the `cdc` mirror; greenfield, no backfill). member_type='learner' (member_ref = learner_id → learners_profiles). PR-staged `feat/cohort-cdc-demote`, NOT applied.
- [x] 4.2 Service/hooks + scoped verify + commit — `TrainingService.addEnrollment` best-effort spine twin (lazy-mint `ensureCdcCohortMirror`, L4 status fold, back-link, `cohort_status_events` audit); routes L5 clean 400/403/409 wraps; `types/cdc/training.ts` link column. No self-serve carve-out (staff-driven enrolment; 03_policies untouched).

## PHASE 5 — Trainer development adapter (🟡 divergent domain)
- [ ] 5.1 Adapter: `sh_training_programs`/`sh_cohort_members` → cohort-core (kind='trainer'); earnings/levels stay extension
- [ ] 5.2 Decide fit vs standalone — fold in LAST, re-evaluate after Phases 2–4 prove the spine (D6)
- [ ] 5.3 Service/hooks + scoped verify + commit

## PHASE 6 — Shared UI + retire duplicates (🟡 cross-module)
- [x] 6.1 Shared cohort UI components — **D9 existing-user picker SHIPPED** (`components/cohort-core/member-picker.tsx`): reusable directory search over `profiles` that can ONLY emit a real `profiles.id` (no free-text submit; Enter is blocked), used across all four domains to feed `member_ref`. Paired with the generic-service guard below. (2026-07-06)
- [x] 6.1b D9 generic member-ref validation — **SHIPPED** in `lib/services/cohort-core/cohort-service.ts`: `CohortService.assertMemberIdentity` rejects a `member_ref` that does not resolve to `profiles.id`/`learners_profiles.id` for `member_type` in `student`/`learner`/`staff` (throws `.status=400`; `team` exempt — its ref is an enrollment id). Called by `createMembership` AND `transferMembership` before any write. Signatures unchanged. Lifts the per-domain SF100/Foundations guard into the shared engine. (2026-07-06)
- [ ] 6.2 Retire/park dead duplicate cohort screens — **DEFERRED** (depends on Phase 4/5 landing the CDC + Trainer roster reads on the spine; retiring a domain roster screen before its reads are cohort-backed would drop live UI). NOT done in the picker PR.
- [ ] 6.3 Scoped verify + commit

## PHASE 7 — THE MOAT (self-improving loop) — spec'd 2026-07-05; BUILD after fuel exists
> **Moat audit (moat-loop skill, 2026-07-05): verdict = NO LOOP today.** Parts 4 (outcome-vs-baseline) + 5 (feed-forward) are ABSENT; 0 fuel (0 graduations, 0 verified paid users, 1 program). The **verified-paying-user** metric is the moat SEED — a hard, external, non-replayable signal. Certify with a REAL 2-cycle sim once cohorts complete; never "verified by construction."

Decisions (interview 3, 2026-07-05):
- **M1 · Success metric** = BLENDED score (success-rate + verified-revenue + speed, **EQUAL weights**). Same fixed estimator on baseline AND outcome (no double-count).
- **M2 · Capture** outcome + baseline AT COHORT CLOSE, from cohort #1. Fold into D7 `closeCohort`. **MUST ship before any cohort closes — a baseline can't be reconstructed later.**
- **M3 · Feed-forward** = system AUTO-ADJUSTS the program (deadlines / targets / mentor assignment) from measured outcomes.
- **M4 · Alumni flywheel** = graduates AUTO-eligible as mentors next cohort (`member_type='staff'`, `role='mentor'`, referencing the graduated member).
- **M5 · When it may act** = auto-adjust allowed after **1** completed cohort…
- **M6 · Reality check** = …but ALWAYS gated by a no-change CONTROL group (within-cohort team-level A/B, since 1 cohort has no prior). This kills the regression-to-mean confound AND is exactly what makes the 2-cycle sim certifiable.
- **M7 · Safety brake** = …and a HUMAN approves each auto-change until trusted, then loosen.

Build items (deferred until fuel / explicit go):
- [x] 7.0 M2 outcome-capture-at-close — **SHIP BEFORE FIRST COHORT CLOSE** — BUILT 2026-07-05 (PR-staged in `feat/cohort-moat-m2-outcome-capture`, NOT applied). Captured via a DATABASE TRIGGER (`fn_capture_cohort_outcome` / `trg_cohort_capture_outcome` on `cohort_memberships`), NOT folded into `closeCohort` — a trigger is the single chokepoint every close passes through, so the baseline can't be stranded by any service/RPC/admin path that forgets. NEW `public.cohort_outcomes` table (migration `20260731091000_cohort_outcome_capture.sql`; institution_id NOT NULL copied from the parent cohort; RLS canonical; partial-unique per membership; NO backfill of pre-trigger terminals). Read/write: `lib/services/cohort-core/outcome-service.ts` + `hooks/cohort-core/useCohortOutcomes.ts`. The **blended-score / baseline VALUE** (M1) lands in `outcome_snapshot` in 7.1.
- [x] 7.1 Blended cohort-outcome metric (equal-weight) + baseline storage; fixed estimator both ends — **BUILT** 2026-07-06 (`20260731092000`): `fn_cohort_blended_score` (`sf100.v1`, both ends, DATA-driven o_hit; REVOKE anon/PUBLIC/authenticated — trigger-only); `fn_capture_cohort_outcome` upgraded to merge the score envelope.
- [x] 7.2 Control-group assignment (within-cohort A/B) + lift calc with confound check — **BUILT** (`20260731093000`): `experiment_arm` on membership.config, `cohort_experiments` table, `fn_assign_experiment_arms_for_cohort` + `fn_compute_cohort_experiment` (INVOKER); causal_lift=treatment−control, naive_lift kept for contrast.
- [x] 7.3 Feed-forward proposer (auto-adjust) gated by M5 (≥1 cohort) + M7 (human approval) — **BUILT** (`20260731094000`): `cohort_adjustment_proposals`, `fn_propose_cohort_adjustments` (INVOKER, M5 gate, pending-only), `fn_apply_cohort_adjustment_proposal` (DEFINER + institution-bound authority gate, M7 approved-only, whitelisted levers).
- [x] 7.4 Alumni→mentor pipeline (graduate → eligible mentor next cohort) — **BUILT** (`20260731095000`): `v_cohort_alumni_mentor_pool` (security_invoker); enrol via `CohortService.createMembership(staff/mentor)`.
- [x] 7.5 CERTIFY with 2-cycle sim + confound check — **CERTIFIED** 2026-07-06: sandboxed 2-cycle sim (rolled-back txn vs prod) proved ctrl=0.400 / treat=0.833 / **causal=0.433 < naive=0.617** (confound removed), decision=adopt, **M7 blocked un-approved apply**, target 5→6 (loop closed), alumni=4, **M5 blocked open-cohort propose**, arms 50/50 — then ROLLED BACK; prod residue verified 0.

## FINAL GATE
- [ ] Run `tsc --noEmit` → 0 new errors in touched files
- [ ] Run `eslint` on touched files → clean
- [ ] Run `npm run build` gates (gen:routes/check:sidebar/check:reachability/check:audit-coverage) → pass
- [ ] Open PR(s) via ship worktree; STOP (human merges + deploys)

## Progress log
- 2026-07-05 10:15 — Worktree `feat/cohort-core` created off jicate/main (@3ad2c343b); SF100 code present; plan written. Next: Phase 1.1.
- 2026-07-05 10:54 — PHASE 1 COMPLETE → **PR #1797**. Built + adversarially verified via 11-agent ultracode workflow (0 confirmed critical/high; tsc 0, eslint 0). Added CHECK on status_events target; verified FK/trigger targets exist in prod. Migration NOT applied (PR-staged). HELD at Phase-1 checkpoint for Director review before Phase 2 (first phase to touch LIVE SF100 data).
- 2026-07-05 — **PHASE 2.3 BUILT — FULL MIGRATION (re-key/repoint only; NO drops), PR-staged, NOT applied.** Supersedes D3 "copy+keep": `cohorts`/`cohort_memberships` are now CANONICAL for program+roster+membership-status; `sf100_enrollments` demoted to an SF100 per-team extension. **Migration** `20260731060000_sf100_demote_to_extension.sql` (branch `feat/sf100-full-migration`): adds ONE nullable `sf100_enrollments.cohort_membership_id uuid` → `cohort_memberships(id)` **ON DELETE SET NULL** (link, not identity), FK guarded on `pg_constraint`, reverse index, `IS DISTINCT FROM` idempotent populate (scoped `kind='sf100'`+`member_type='team'`+`member_ref=e.id`), `RAISE NOTICE` self-check, `NOTIFY pgrst`. Link-column beats re-key: all 10 SF100 extension tables keep FK-ing `sf100_enrollments.id` unchanged (zero data rewrite), cohort-core reached in one hop. **NOT NULL + DROPs deferred** to a later follow-up gated on live-verify (`COUNT(*) WHERE cohort_membership_id IS NULL = 0`) + sign-off. Mirrored as a guarded ALTER into `setup/01_tables.sql` + registered in `SQL_FILE_INDEX.md`. **Repointed 6 files** (`sf100-service.ts`, `pipeline-service.ts`, `leaderboard/route.ts`, `leaderboard/stats/route.ts`, `seed-from-declarations/route.ts`; `roster-changes/pending/route.ts` = documented no-op) — roster reads go cohort→membership→enrollment (`member_ref`) with **legacy `program_id`/`sf100_programs`-column fallback on every read** (safe to deploy before apply; no RLS regression); program config from `cohorts.config`; `enrollTeam` write-side twin. Behaviour held identical (status filters kept on the extension; program CRUD left on `sf100_programs`). **NOT applied; NOT committed** — held for scoped verify + Director sign-off. **Live-test gate (gotcha #9):** SF100 roles need `cohort.view` (reads) and `cohort.create` (enrollTeam twin) or the cohort path silently under-returns — must be re-tested under an actual SF100 role before the DROP follow-up.
- 2026-07-06 — **D9 SF100 roster profile-required guard BUILT** (added to **PR #1814**, PR-staged, NOT applied). Migration `20260731070000_sf100_roster_profile_required.sql` — additive/idempotent (guarded on `pg_constraint`, 0 rows today): CHECK `sf100_roster_changes_identity_required` = `profile_id IS NOT NULL OR learner_id IS NOT NULL`. **Made non-breaking by fixing the ONLY INSERT path first** (research flagged it as UNSAFE-as-is: `requestRosterChange` was designed to insert email-only rows, both identity links NULL — the live `'remove'`-by-email and external-`'add'` cases): `lib/services/startup-studio/sf100-service.ts::requestRosterChange` now resolves identity (explicit DTO → matching `event_team_members` row's `profile_id`/`learner_id` → email lookup vs `profiles.email` then `learners_profiles.student_email`/`college_email`) and throws `.status=400` if nothing resolves; the API route `.../roster-changes/route.ts` wraps the call to return a clean **400** instead of a masked 500. Signatures unchanged. Mirrored as a guarded ALTER into `setup/01_tables.sql`; registered in `SQL_FILE_INDEX.md`. Enforcement (a) picker-only UI + (b) generic `cohort-service` member-ref validation remain **Phase 6**. `tsc`/`eslint` scoped-clean on touched files.
- 2026-07-06 — **PHASE 3 BUILT — Foundations onto the core (PR-staged, NOT applied).** ADDITIVE fold mirroring SF100 Phase 2.3 (no drops, no re-key). **Migration** `20260731080000_foundations_demote_to_cohort_core.sql` (idempotent, drops-nothing; both `ss_foundations_cohorts`/`_enrollments` = 0 rows → empty-table NOTICE, NO backfill/populate/abort): ONE nullable `ss_foundations_enrollments.cohort_membership_id uuid` → `cohort_memberships(id)` **ON DELETE SET NULL** (pg_constraint-guarded) + reverse index `idx_ssf_enroll_cohort_membership`; **D9 CHECK** `ss_foundations_enrollments_student_required` (`student_id IS NOT NULL`); **self-enrol RLS carve-out** `cohort_memberships_foundations_self_insert` (member_type='student' AND member_ref=auth.uid() AND non-terminal AND parent kind='foundations') — gotcha #1: self-enrol runs AS THE STUDENT (never service-role) and would 403 the membership half without it. `NOTIFY pgrst`. Mirrored → `setup/01_tables.sql` (ALTER) + `setup/03_policies.sql` (carve-out) + `SQL_FILE_INDEX.md`. **`ss_foundations_cohorts` deliberately NOT dropped/re-keyed (gotcha #2, largest blast radius):** worksheets/responses FK it + listWorksheets override keys off it, so it stays the authoritative DOMAIN container; `cohorts` (kind='foundations') is an ADDITIVE spine MIRROR (`config->>'ss_foundations_cohort_id'`) minted forward in the service — so container reads + roster stay on `ss_foundations_*`, **UI signatures unchanged**. **Service** `foundations-service.ts`: `ensureFoundationsCohortMirror` + `mirrorEnrollmentToSpine` (best-effort twins, exactly like SF100 `enrollTeam`); `createCohort` mints mirror; `enroll` gains **D9 profile-resolve guard (clean 400)** + membership upsert (member_ref=student_id) + back-link + 'enrolled' event + self-heal for existing rows; `withdraw` also flips membership→'removed'; new **`signOffGraduation`** (D5): assert global completion ≥ threshold → enrollment `completed` → best-effort membership→'graduated' + event + notify. Written server-side on `this.supabase` (NOT the client-only `CohortService`). **Routes:** facilitator-add + self-enrol wrap the D9 400; new `POST …/foundations/enrollments/[id]/complete` gated `startup_studio.foundations.review`. `FoundationsEnrollment` type gains optional `cohort_membership_id`. Removed a colliding untracked draft (`…070000_foundations_demote_to_extension.sql` — timestamp-clashed with the committed `…070000_sf100_roster_profile_required.sql`). **Scoped-green:** tsc 0 new errors on touched files, eslint 0. NOT applied; NOT explicitly committed (worktree auto-save hook wrote 'wip' commits). **Live-test gate (gotchas #1/#9):** self-enrol needs the carve-out live; facilitator membership-mirror needs the manage role to also hold `cohort.create` (else roster still authoritative from the extension); status-event/graduation audit inserts need `cohort.edit` (best-effort, non-fatal). **Hardened after an independent 5-dimension adversarial review (5 CONFIRMED + 1 verified):** (1) **[HIGH]** the D5 `/complete` route now sets `allowApiKey:false` — `withAuth` skips `requirePermission` for api_key callers (with-auth.ts:192), so a write-scoped key could have self-ratified graduation, bypassing the mentor gate; (2) **[MED]** `enroll()` now enforces the cohort's `enrollment_mode` on the self path (a student can no longer self-join a `facilitator`-only cohort); (3) **[MED]** added partial unique index `uq_cohorts_foundations_ss_id` on `cohorts((config->>'ss_foundations_cohort_id')) WHERE kind='foundations'` + 23505-recovery in `ensureFoundationsCohortMirror` — closes a concurrent-enrol duplicate-mirror race that self-compounded (swallowed multi-row lookup error minted a new mirror on every subsequent enrol); (4) **[LOW]** `mirrorEnrollmentToSpine` folds membership status from the authoritative extension status (withdrawn→removed, completed→graduated) instead of hardcoding 'enrolled', so a re-mirror can't resurrect a terminal enrollee; (5) **[LOW]** `enroll()` insert now surfaces an RLS denial (42501) as a clean 403 not a masked 500; (6) **[LOW]** aligned the graduation-threshold docstrings to the shipped `GRADUATION_DEFAULT_PCT=100` (Director 2026-06-22: Explorer = ALL canon worksheets). Migration re-dry-ran clean; eslint 0 on touched files.
- 2026-07-05 — PHASE 2.0–2.2 BUILT (engine + backfill, PR-staged, NOT applied). **Engine (D2/D7/D8):** `lib/types/cohort-core.ts` adds `CohortRuleConfig`/`CohortConfig`/`CohortCloseStatus`/`TransferMembershipDto`/`CloseCohortDto`; `lifecycle.ts` adds pure `defaultRuleConfigForKind(kind)` (sf100+foundations = full lifecycle; cdc+trainer = inactivity-only), `isRuleEnabled`, `membershipCloseStatus`, `isTerminalMembershipStatus/isTerminalCohortStatus`; `cohort-service.ts` adds `transferMembership` (re-points cohort_id, keeps history + 'transferred' event, blocks terminal rows) and `closeCohort` (container→completed/archived + guarded/audited member cascade); hooks add `useTransferMembership`/`useCloseCohort`. No Phase-1 API broken (append-only). **Backfill:** `20260731050000_cohort_core_sf100_backfill.sql` — COPY-only, non-destructive, idempotent (1 program→cohort, 18 enrollments→team memberships). Status maps: program enrollment_open→enrolling; enrollment warning/probation→active (original kept in config.sf100_status), withdrawn→removed. DATE→timestamptz at IST boundaries. sf100_* left intact (D3, no cutover). **2.3 (repoint SF100 reads) still pending — held for Director sign-off.**
- 2026-07-06 — **PHASE 6 (spine-only) BUILT — D9 generic member-ref guard + shared existing-user picker.** Code+UI only, NO schema change. (1) `lib/services/cohort-core/cohort-service.ts`: new `CohortService.assertMemberIdentity(member_type, member_ref)` — for identity-bearing types (`student`/`learner`/`staff`) resolves `member_ref` against `profiles.id` then `learners_profiles.id`; rejects free-text / unresolvable / invalid-uuid (`22P02`) with a clean `.status=400`; a genuine non-`22P02` DB/RLS error is rethrown (never masked as a false 400). `team` is EXEMPT (its `member_ref` is the team enrollment id, not a profile — leaves the SF100 per-team path intact). Wired into BOTH `createMembership` (member-add) and `transferMembership` (never carry a free-text member into a new cohort) BEFORE any write. Signatures unchanged (back-compatible). Lifts the per-domain SF100 `requestRosterChange`/Foundations guard into the shared engine. (2) `components/cohort-core/member-picker.tsx` (NEW): reusable `<MemberPicker>` — debounced directory search over `profiles` (co-located `useMemberDirectorySearch` hook, no touch to `hooks/cohort-core/index.ts`), out-of-order-response guarded, PostgREST `or()`-term sanitized, `is_active` + optional `institutionId`/`roles`/`excludeIds` filters. Emits ONLY a real `profiles.id` via `onSelect` — no free-text submit, Enter is blocked (`preventDefault`), controlled `value` chip + optional `onClear`. Safe under RLS: `profiles_select_policy` = any authenticated user can search the directory, so the guard can never false-reject a picker-chosen member. **Retire-duplicate-roster-screens (6.2) DEFERRED** — depends on Phase 4/5 landing CDC+Trainer reads on the spine; NO existing screen retired in this PR. `eslint` 0, differential `tsc` 0 on both touched files.
- 2026-07-06 — **PHASE 7 M3–M7 BUILT + CERTIFIED — THE MOAT (self-improving loop), PR-staged (`feat/cohort-moat-m3-m7`), NOT applied.** 4 migrations (092000 blended-score+baseline / 093000 control-group+causal-lift / 094000 feed-forward proposer+M5+M7 / 095000 alumni-mentor view), all TIER-1 additive/idempotent. Security-hardened after self-review: blended-score fn REVOKEd from authenticated (trigger-only, cross-tenant revenue leak); compute/propose/assign → SECURITY INVOKER (caller RLS scopes tenant, no DEFINER bypass); apply stays DEFINER with an internal authority gate bound to the PROPOSAL's institution (never a caller param) + service-null-trust. **Certified by a sandboxed 2-cycle sim** (all 4 migrations + seed + 2 cycles + assertions run in ONE implicit transaction aborted via RAISE → prod untouched, returns summary): control_lift=0.400, treatment_lift=0.833, **causal_lift=0.433 < naive_lift=0.617 (confound removed)**, decision=adopt, M7 blocked un-approved apply, program target 5→6 (loop closed — cycle N+1 faces the adjusted target), alumni pool=4, M5 blocked open-cohort propose, arms split 6/2-distinct. Re-ran green after the security hardening; prod residue verified 0. TS: services `{experiment,feedforward,alumni-mentor}-service.ts`, hooks `useMoat.ts`, types appended. Mirrored into `setup/{01_tables,03_policies,04_triggers,05_views}.sql` (+ 02_functions pointer) + SQL_FILE_INDEX. **Admin approval UI (M7 human surface) = documented follow-up** (deferred to keep the PR migration+service focused and clear of the new-module build gates). Next: human review → merge → apply migrations (Management API) → live-verify a real close captures a scored outcome.
- 2026-07-06 — **PHASE 7 M3–M7 HARDENED after a 3-lens adversarial panel (security / score-math / loop-logic).** 3 ship-blockers + several mediums fixed, then re-certified (12 assertions incl. new negatives, all green, rolled back). **CRITICAL cross-tenant WRITE:** `fn_apply` now re-derives authority from the TARGET program's own institution and binds the UPDATE `AND institution_id = v_p.institution_id`; `fn_propose` rejects a config-supplied program_id in another tenant (member_ref/config.sf100_program_id are attacker-writable, previously unbound). **HIGH cross-tenant READ:** `fn_cohort_blended_score` gains a `p_institution_id` param + refuses a member_ref whose program is in a different institution; raw revenue stripped from the snapshot `inputs`. **CRITICAL double-apply (C1):** additive delta could stack via re-propose (index only covered `pending`) or `applied→approved` re-transition — fixed with idempotency index over `pending`+`applied`, an `applied_at`-guard in apply, and a BEFORE-UPDATE terminal-state trigger (`fn_cohort_proposal_guard`, also binds `reviewed_by=auth.uid()` anti-spoof; `applied_by=auth.uid()`). **Math:** H1 lower-clamp `GREATEST(0,LEAST(..,1))` on all 4 components (negative net-revenue no longer drags blended <0); M4 dynamic denominator (drop revenue axis when `min_transaction_amount` is 0/NULL instead of feeding a silent 0); M3 `fn_cohort_min_arm_n()=3` gate (causal_lift NULL below 3/arm → M5 refuses noise); M1 version-filter the experiment aggregation. Deadline shift floored at today (IST). Whitelist-validated `treatment_params` in propose (non-promotable key → inconclusive, no silent no-op). Alumni view REVOKEd from anon. `is_admin()` systemic bypass + proposer≠approver separation-of-duties = documented follow-ups (not this PR).
